---
description: Write or refresh HANDOFF.md at the project root through the handoff skill
argument-hint: "[optional: what changed, what to flag for the next session]"
allowed-tools: Skill, Bash, Read, Write, Edit
---

Invoke the `handoff` skill.

Raw slash-command arguments (context for what the next session needs to know,
if given):
`$ARGUMENTS`

REVIEW OF RECORD: tmux-teams v0.33.0
TASK: v033-review-luna-2

Scope and evidence

I read the committed v0.32.0..HEAD bytes selected by node scripts/gate-required.mjs.
The range is 22 commits and 25 changed files; the gate exits 2 and names 20
deciding files. The exact scoped Node run completed 292 tests, 292 passed, 0
failed, 0 skipped, after the sandbox could not create its inherited macOS temp
directories and the run was repeated with TMPDIR=/tmp outside that restriction.
That green run is test evidence, not release approval. During the inspection an
uncommitted comment-only change appeared in tests/acp-lanes-mcp.test.mjs; I did
not create or revert it, and it is not part of the committed gate range.

VERDICT: BLOCKED

Round-table

John (PM / release gate): The gate is REQUIRED, not waived. I accept that the
runtime source satisfies properties 1 through 6 on the bytes I read, and that
the blocked-terminal behavior is covered by real consumer tests. I do not accept
292/292 as closure: the review contract explicitly requires call-site mutation
evidence, and the repository itself records surviving call-site mutants. Sally,
the documentation contradiction is also release-facing, not editorial polish.

Sally (UX / documentation): John is right about the operator-facing boundary.
ROADMAP.md is unusually honest: lines 269-279 say item 5 is HALF and that nobody
has completed a real Claude Max login. But README.md:305-309 still says there is
no channel for a person to type into a running worker and that companion stdin
is not a human-input path. acp-companion.mjs:3028-3053 explicitly installs that
stdin bridge in login mode. The unqualified README sentence is false for the
new supported mode. ROADMAP.md:271-273 also says all five terminal methods are
under test, while tests/acp-terminal-capability.test.mjs:137-169 exercises
create, wait_for_exit, output and release, not terminal/kill. That sends an
operator to a capability whose proof is overstated. I call both documentation
defects blocking.

Grumbal (The Adversary / mutation reviewer): The source is safer than the test
contract. These guards have no negative test at their production call site:

- acp-companion.mjs:64-67 rejects malformed ACP_SPAWN_NONCE, but
  tests/acp-dispatch.test.mjs:507-525 only proves a valid nonce is echoed.
  Removing the malformed-input guard is not exercised.
- acp-companion.mjs:97-100 rejects invalid ACP_ENABLE_TERMINAL values, but
  tests/acp-terminal-capability.test.mjs:101-121 only uses absent and 1.
- acp-companion.mjs:1469-1475 applies the child-side prohibited-model guard,
  but tests/acp-dispatch.test.mjs:2396-2438 stops in the parent and
  tests/review-policy.test.mjs:743-769 are source-text assertions. A child
  guard removed while the parent guard remains would leave this suite green.
- acp-dispatch.mjs:803-804 applies belongsToThisRun during admission. The
  admission fixture at tests/acp-dispatch.test.mjs:1709-1723 uses a matching
  nonce, so deleting that call still refuses the active lane. The forged and
  missing-nonce cases at :472-505 exercise statusReport, not admission.
- acp-dispatch.mjs:1033 applies belongsToThisRun in watchBoot. The test itself
  admits the survivor: tests/acp-dispatch.test.mjs:1865-1873 says mutating the
  boot identity gate leaves the test green because retirement removes the ghost
  before boot reads it.
- acp-companion.mjs:3000-3017 adds the terminal output byte cap and UTF-8 tail
  boundary, but the terminal test only asserts an under-limit output with
  truncated=false at tests/acp-terminal-capability.test.mjs:157-160. No test
  crosses the cap.

Those are blocking under property 7, even though the helpers are sensible.
The comments are not a substitute for red mutation evidence.

Luna (ACP / runtime): I disagree with treating the source properties as failed.
For property 1, acp-dispatch.mjs:695-723 checks both ACP_MODEL and
ACP_EXPECT_MODEL after only pure task-id validation and before predecessor
retirement, spawn, or unref; the dispatch behavior test covers prohibited and
permitted models. For property 2, acp-companion.mjs:87-102 and :4094-4107 omit
terminal entirely unless ACP_ENABLE_TERMINAL=1, while :3163-3197 returns a
JSON-RPC error for terminal/* when disabled; tests/acp-terminal-capability
proves the wire and request paths. For property 3, the nonce comparison at
acp-dispatch.mjs:973-1001 requires a nonempty expected nonce and exact
spawn_nonce equality in addition to time bounds; the missing/forged cases at
tests/acp-dispatch.test.mjs:444-462 and :472-505 are real.

Property 4 also holds in the state machine: loop-runner.mjs:1544-1558 returns
action escalate for blocked and sends ordinary failures through want(role);
pull-controller.mjs:61-72 says blocked needs a person, while another terminal
still says rerun. The consumer tests at loop-occupancy.test.mjs:1744-1780 and
pull-route-held.test.mjs:166-193 preserve that distinction. My disagreement is
classification: the source routes to the outer controller/PM, not a proven
human interaction. That is a residual evidence limit, not a code failure.

For property 5, acp-lanes-mcp.mjs:420-431, :479-599 and :680-695 show that
endpoint contact is inside the probe transport reached from the named tool
handler, with only per-call timeout timers and no scheduler. The ADR amendment
is real at ADR 0007:9-18 and :61-70, and its no-timer/no-sweep boundary is
explicit at :224-242. The fake transport plus stub-agent tests prove routing
and transport shape, but no live provider call; the ADR and roadmap say so.
For property 6, pulse-data.mjs:128-148 requires work_observed and
:224-230 permits agent_id and spawn_nonce as optional keys, matching
acp-companion.mjs:1618-1625. The real companion snapshot test is at
tests/loop-smoke.test.mjs:292-327.

John to Grumbal: I accept the five call-site findings as blocking because the
brief made that a release property, not because every defensive helper needs a
new test by taste. Luna to John: the exact behavior is not broken, and the
green run should remain visible as evidence. Neither of you has proven a real
Claude Max OAuth login; Sally's wording preserves that boundary.

Property-by-property record

1. HOLDS, blocking issue absent in the committed source. Evidence:
   acp-dispatch.mjs:716-723 places both checks before any spawn call; the
   prohibited/allowed dispatch test is tests/acp-dispatch.test.mjs:2396-2438.
   The child-side duplicate remains independently untested as listed under
   property 7.

2. HOLDS in code and tested mechanism. Evidence:
   acp-companion.mjs:97-102 validates the opt-in and :4094-4107 conditionally
   spreads terminal:true, never terminal:false; :3163-3197 refuses terminal/*
   when disabled and :3544-3548 routes those requests there. The five terminal
   tests pass in the scoped run. A real Claude adapter login is not verified.

3. HOLDS for dispatcher-created runs. Evidence:
   acp-dispatch.mjs:973-1001 requires both timestamp bounds and a real matching
   nonce; acp-dispatch.mjs:913-916 injects it and acp-companion.mjs:1618-1625
   echoes it conditionally. The tests at acp-dispatch.test.mjs:444-462,
   :472-526 pass. The admission/watchBoot call-site gaps remain property 7.

4. HOLDS as a state-machine decision. Evidence:
   loop-runner.mjs:1544-1558 escalates TEAM_BLOCKED instead of calling want;
   pull-controller.mjs:63-72 distinguishes blocked/person from other-terminal/
   rerun. The direct consumer tests pass at loop-occupancy.test.mjs:1744-1780
   and pull-route-held.test.mjs:166-193. No real person interaction was run.

5. HOLDS by source and test transport. Evidence:
   acp-lanes-mcp.mjs:680-695 only probes named lanes from probeLanes, and
   :742-769 binds the third tool to that handler; there is no interval or
   automatic call. ADR 0007 is amended in place at :9-18 and :195-242. The
   provider endpoint itself was not contacted; the stub/fake transport tests
   are deliberately not live-provider proof.

6. HOLDS. Evidence:
   pulse-data.mjs:137-148 includes required work_observed and :224-230 lists
   spawn_nonce only in the optional set. The companion writes those shapes at
   acp-companion.mjs:1610-1625, and tests/loop-smoke.test.mjs:302-327 validates
   an actual companion-produced snapshot. The test run was 292/292 green.

7. FAILS_COMPLETION_CONTRACT / BLOCKING. The guard call-site contract is not
   met. The exact blocking gaps are the six entries Grumbal listed above:
   malformed ACP_SPAWN_NONCE, invalid ACP_ENABLE_TERMINAL, child-side model
   refusal, admission-side nonce binding, watchBoot nonce binding, and the
   terminal output cap/UTF-8 boundary. At minimum the first five need negative
   consumer tests; the boot survivor is explicitly acknowledged by the test
   itself. A pure helper pass or source regex is not enough.

8. BLOCKING documentation mismatch, with important parts correct. ROADMAP.md
   is honest about item 5 being half and no real Claude Max login at :264-288;
   it also states the on-demand probe at :290-301 and the unmeasured real MCP
   host at :433-439. agy-advisor/SKILL.md:52-64 and :86-96 accurately state
   the Gemini 3.1 prohibition and party contract. ADR 0007 records its probe
   amendment. But README.md:305-309 contradicts the opt-in terminal stdin
   bridge at acp-companion.mjs:3028-3053, and ROADMAP.md:271-273 overstates
   that all five terminal methods are under test because terminal/kill is not
   exercised. These must be corrected or explicitly narrowed before release.

Bottom lines

John: BLOCK. The required gate is active, and properties 7 and 8 are release
contract failures even with a green suite.

Sally: BLOCK. Keep the roadmap's explicit "no real Claude Max login" sentence;
fix the README and do not claim terminal/kill is proven until it is exercised.

Grumbal: BLOCK. Remove each listed call site in a mutation run and demand red
consumer tests; the current source comments confess survivors.

Luna: BLOCKED on evidence and prose, not on properties 1-6's implementation.
The runtime source is largely sound, but the release record must not overclaim.

TEAM_BLOCKED v033-review-luna-2

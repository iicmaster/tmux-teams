# แผนพัฒนา tmux-teams — แผนนี้สร้างเสร็จแล้ว

> **หน้านี้เคยเป็นแผน ตอนนี้เป็นบันทึกว่าเกิดอะไรขึ้น** · เขียนครั้งแรก 8 ส.ค. 2026
> สำหรับรีลีสถัดจาก v0.16.0 · ปิดในรีลีส 0.30.0 เมื่อ 14 ส.ค. 2026
>
> ต้นฉบับอยู่ที่ `RELEASE-PLAN.md` ในเรพ และหน้าเว็บคือการเรนเดอร์ของไฟล์นี้
> ก่อน 14 ส.ค. หน้านี้เป็น HTML ที่เขียนด้วยมือ ไม่มีต้นฉบับ ไม่มีอะไรรู้ว่ามันเก่า
> — และมันเก่าไปเจ็ดวันโดยไม่มีใครเห็น

## สิ่งที่แผนนี้บอกว่าจะทำ

> *"เรากำลังทำให้ระบบ **หยุดเมื่อมีปัญหา** ได้จริง — งานติดที่ทีม = WIP ค้างที่ทีม ·
> escalate มาที่ PM = WIP ของ PM ค้าง · แต่โค้ดไม่เคยทำได้"*

ปัญหาที่ระบุไว้ตอนนั้นมีสองชั้น และทั้งสองชั้นปิดแล้ว

**ชั้นแรก: ระบบถามได้ แต่ไม่มีใครตอบได้** — ระบบเขียนคำถามถึงคนได้ห้าจุด กระดานวาดว่า
"กำลังรอคนตอบ" ตัวตรวจรับคำว่า `answered` แต่ไม่มีทางให้คนตอบจริง ๆ
· **ปิดด้วย `answer.mjs`** ซึ่งเป็นประตูให้คนตอบคำถามที่ระบบเขียน

**ชั้นที่สอง: ที่นั่งของ PM ไม่เคยมีอยู่จริง** — และนี่คือชั้นที่แผนเดิมยังไม่รู้
· การนับที่นั่งเป็นการ *อนุมาน* จากประวัติของโทเค็น และ **การอนุมานถืออะไรไว้ไม่ได้**
· งานที่เสร็จแล้วแต่ยังไม่ถูกตรวจ กับงานที่ escalate ขึ้นมา **ไม่ได้อยู่ในมือใครเลย**
· ที่นั่งเดียวของ control จึงว่างตลอดเวลาที่มันติดหนี้คำตัดสิน ประตูหน้าเปิดรับงานใหม่เข้ามาติดต่อ
· **และนั่นคือเหตุผลที่ตัวจับเวลาแปดตัวงอกขึ้นมาแทนกลไกหยุด**

## สิ่งที่เกิดขึ้นจริงในรีลีส 0.30.0

```
publisher     ledger + appendEvent      ประตูเขียนเดียว · 8 ผู้เรียก · 0 bypass
subscribers   token · team · workflow · display
teamOccupancy เรียกโดเมน — ตัวอ่านทุกตัวได้คำตอบเดียวกัน (สัญญาข้อ 13)
RELEASING_EVENTS  เหลือสองคำ: audited · abandoned
escalated     ถือสองที่นั่ง — ทีมส่งงาน และ control
completed     เป็นคิวของ control จนกว่าจะถูกตรวจ
ประตูหน้า      ปฏิเสธขณะ control ติดหนี้คำตัดสิน  ← สัญญาณเตือน
```

ห้าจากหกช่องในตารางการติดตาม event ต่อสายแล้ว ช่องที่หกเป็นตัวเฝ้าโดยตั้งใจ
· เหตุผลอยู่ใน `ROADMAP.md` และในโค้ดที่จุดนั้น

## สิ่งที่แผนนี้ไม่ได้ทำ และยังไม่มีใครทำ

**ตัวจับเวลามีเจ็ดตัว ไม่ใช่แปด และหลักฐานรายตัวผลิตแล้ว 15 ส.ค. — ไม่มีตัวไหนถอดได้**
· `MAX_AUDIT_TRANSPORT_RETRIES` ถูกถอดไปตั้งแต่ 7 ส.ค. คอมเมนต์ของมันเองบอกไว้
เอกสารสามใบยังนับแปดต่ออีกสัปดาห์
· วิธีวัด: ปิดทีละตัว แล้วรันเทสลูป 216 ตัว → `ANSWER_DEADLINE_SEC` 9 แดง · `ZOMBIE_SEC` 7
· `MAX_ATTEMPTS` 4 · `MAX_LEGS` 2 · เบรกทริกเกอร์ 2 · `MAX_IN_FLIGHT` 1 · `PM_COOLDOWN_SEC` **0**
· **ไม่มีตัวไหนยืนแทนที่นั่ง PM เลย** — WIP hold ตอบว่า "โทเค็นนี้ถือที่นั่ง control อยู่"
ส่วนเจ็ดตัวนี้ตอบคนละคำถาม: นาฬิกา · โพรเซสที่ตาย · ความยาวเส้นทาง · อัตราการจ่ายเอเจนต์
· **0 แปลว่าไม่มีใครเฝ้า ไม่ได้แปลว่าซ้ำซ้อน** — เขียนการ์ดให้แล้ว ถอดเบรกเมื่อไหร่มันแดง

## แผนถัดไปอยู่ที่ไหน

`ROADMAP.md` — เฟส D (แยกโดเมนออกจาก `nextStep`) และเฟส F (สคริปต์ก่อน/หลัง LLM ต่อตำแหน่ง)
ยังเปิดอยู่ · หน้านี้จะไม่ถูกเขียนต่อในฐานะแผน มันปิดแล้ว

# HANDOFF — tmux-teams · v0.17.0 ออกแล้ว (2026-08-08)

> เขียนให้ **agent** อ่านแล้วลงมือต่อได้ทันที
> อ่านสามหัวข้อแรกแล้วรู้ว่าต้องทำอะไรต่อ

## อ่านก่อนอื่น

- **อยู่บน `main`** · **v0.17.0 ปล่อยครบทุกช่องทางแล้ว** — push, tag, GitHub release,
  marketplace, plugin cache · `origin/main` ตรงกับ local
- **2 คอมมิตหลังรีลีสยังไม่ push:** `24ca2f3` (ปฏิเสธฟิลด์ที่ไม่ได้อ่าน) และ
  `fb52747` (ลูปตื่นเมื่อไฟล์เปลี่ยน) · **suite 782 fail 0**
- **ยังไม่ commit:** `docs/` (สำเนา artifact — ไม่อยู่ในรายการ tracked ของ repo นี้)
- **เอกสารสองชุด:** สถาปัตยกรรม `.../tmux-teams-ddd-reading/` และแผนงาน
  `.../tmux-teams-next-plan/` · ไฟล์ทำงานอยู่ใน `docs/artifacts/claude/private/`
  · **ต้องมี `<meta charset="utf-8">` เสมอ** — สคริปต์ artifact-sftp ไม่ห่อ head ให้
- **9 อิชชูเปิดอยู่** · ปิดไป 2 ใบใน v0.17.0 (audit ตายเพราะโควตา · งานตีปิงปองสองทีม)

## v0.17.0 — ประตูของคนครบสามบาน

| คอมมิต | เรื่อง |
|---|---|
| `9fecd40` | `answer.mjs` — **ลูปถามคนได้มาตลอด แต่ไม่มีใครเขียน `answered` ได้เลย** |
| `13b404c` | retry ของ audit มีอยู่เพราะไม่มีใครตอบได้เท่านั้น |
| `56d00e9` | คำถามที่ไม่มีที่นั่ง หยุดอะไรไม่ได้ อย่าเขียน |
| `7dab151` | **D6** ทีม control บังคับ — 212 เทสใน 17 ไฟล์ |
| `83b93a0` | `withdraw.mjs` — คนปิดโทเค็นได้ |
| `bbb804e` | เลน deepseek + เส้นทางรีวิวที่มีสำรอง |
| `55b6206` | 12 จุดที่แผงรีวิวจับได้ รวม B1 ที่บล็อก |

## เกตรีลีส — กฎเปลี่ยนแล้ว อ่านก่อนปล่อยรอบหน้า

**เกต sandbox (`review-gate.mjs`) รันบนเครื่อง Mac นี้ไม่ได้** — ทุกโปรไฟล์ประกาศ
`osSandbox: 'bwrap'` และ `acp-review-client.mjs:632` ปฏิเสธถ้าไม่ใช่ Linux
· **อาการ: สามเลนล้มพร้อมกัน `stage: config` · `stderrBytes: 0`** อ่านย่อหน้านี้ก่อนเชื่อว่าเลนพัง

**บน macOS ใช้ ACP ตรง** (`acp-companion.mjs` · ไดเรกทอรีแยกทุกเลน) · **จด
`effective_identity` ทุกเลน** — กฎเต็มอยู่ใน `CLAUDE.md` Release flow ข้อ 2

**packet จำกัด 128 KiB** — diff รีลีสเกินแน่ · **แบ่งตามความหมาย ห้ามขยายลิมิต**

**เลนรีวิว:** agy (gemini) · zai (glm ผ่าน api.z.ai) · qwen (qwen3.8-max ผ่าน aliyuncs)
· deepseek = `claude-qwen --model sonnet` **แทน qwen ที่ล้มได้ แต่นั่งข้าง qwen ไม่ได้**
· **codex กลับมาแล้ว (Master แจ้ง 8 ส.ค.)** — ยังไม่เอากลับเข้าเส้นทาง claude

## แผนงานถัดไป — ผมเขียนผิดสามในห้าข้อ อ่านก่อนทำต่อ

**วัดด้วย subagent สามตัว 8 ส.ค. · สามข้อในแผนเดิมตายเพราะหลักฐาน:**

| เคยเขียนว่า | ความจริง |
|---|---|
| `workflow` ไม่เป็นโดเมน | **เป็นอยู่แล้ว** — `pull-controller.mjs:154-200` เดินเส้นทางถูกต้อง |
| รันเนอร์พึ่งหน้าจอ | **ไม่จริง** — สัญญา §11.1 บอกเองว่า `pulse.json` คือหลักฐาน |
| ทีมต้องประกาศกระบวนการเอง | **ยังไม่จำเป็น** — ไล่ 9 ใบแล้วไม่มีใครขอ |

**เหลือ `team` โดเมนเดียวที่ฮาร์ดโค้ด (8 จุดใน `nextStep`) และไม่มีใครต้องการให้เปลี่ยน**

**MQ: ทำแล้วบางส่วน** (`fb52747`) — และ**ไม่ได้ใช้ broker** · แหล่งอีเวนต์จริงมี 3 ทาง:
worker เขียน outbox · คนสั่งผ่าน 3 ประตู · นาฬิกา (เส้นตาย 5 ตัว) · สองในสามคือไฟล์เปลี่ยน
· **interval ยังอยู่เพราะเป็นผู้อ่านเดียวของนาฬิกา** · POC ใน scratchpad พิสูจน์
at-most-once หาย 1/60 — **จริงแต่ไม่ใช่ปัญหาของเรา** สมุดต่อท้ายอย่างเดียวหายไม่ได้

### หลักการตัดสินใจของโปรเจกต์ — Master, 2026-08-07

> **ทำสิ่งที่จำเป็น ในเวลาที่จำเป็น เท่าที่จำเป็น**

สามด่านแยกกัน: **จำเป็นไหม** · **ต้องตอนนี้ไหม** · **เท่านี้พอไหม**
รันคำถามผ่านสามด่านนี้**ก่อน**เอาไปถาม Master — วันนี้มันตอบให้เองเกินครึ่ง
มันไม่ตอบคำถามเรื่อง**ข้อเท็จจริง** (มีกี่โดเมน · โค้ดบรรทัดนี้ทำอะไร) — พวกนั้นต้องวัด

### หัวใจของระบบ — Master, 2026-08-06

> งานติดที่ทีม = โทเค็นอยู่ในทีม = WIP ค้างที่ทีม · **escalate ขึ้น PM = PM ทำงาน = WIP
> ของ PM ค้างจนเสร็จ ไม่ว่างานอะไร** · Kanban พื้นฐาน · **ทรงพลังเพราะทำให้ระบบหยุด
> เมื่อมีปัญหา**

**วัดใหม่ 2026-08-07 — แม่นกว่าเดิม ใช้ตัวนี้:**

| กลไก | กินช่องของใคร | ตรงกับที่ Master พูดไหม |
|---|---|---|
| `questioned` | **ทีมที่ถาม** — ไม่อยู่ใน `RELEASING_EVENTS` และวางด้วย `teamOf(agent_id)` | ✅ ตรง |
| `escalated` | **ทีมเดิม** — เขียน `to_team` เป็นทีมที่ถืออยู่โดยตั้งใจ | ❌ ไม่กิน PM |
| `audit_requested` / `audit_lost` | ไม่กินใคร — อยู่ใน `RELEASING_EVENTS` | — |

**ความขัดแย้งที่เป็นแก่นของ STEP C:** `loop-runner.mjs:2141` คอมเมนต์ว่า
*"The controller is not a team"* แล้ววาง escalate กลับทีมเดิม · แต่
`workflow-graph.mjs:149` **สร้างทีม `control` จริง โดยมี `worker_ids: [controllerId]`**
→ `teamOf(controller) === 'control'` · **โค้ดสองที่พูดคนละเรื่อง ต้องตัดสินก่อนเขียน STEP C**
ในที่ว่างนั้นงอกเบรกแปดตัวใน `loop-runner.mjs` — แต่**มีตัวเดียวที่ช่อง WIP แทนได้ไม่มีเงื่อนไข**
(เส้นตายของ audit) รีวิวภายนอกตรวจทีละตัวแล้ว อย่าเชื่อประโยค "แปดตัวคือของทดแทน"

## สี่โดเมนตามที่ Master นิยาม — วัดใหม่ 8 ส.ค. หลัง v0.17.0

| โดเมน | เป็นเจ้าของ | สถานะ |
|---|---|---|
| **token** | `work-items/*.jsonl` | **ครบแล้ว** — ประตูของคนสามบาน: `admit` · `answer` · `withdraw` |
| **team** | โครงสร้าง + วิธีทำงานภายในทีม | **ฮาร์ดโค้ด 8 จุดใน `nextStep`** — และ**ไม่มีใบปัญหาไหนขอให้เปลี่ยน** |
| **workflow** | เส้นทางระหว่างทีม | **เป็นเจ้าของการส่งต่อแล้ว** — `pull-controller.mjs:154-200` |
| **display** | `pulse.json` + 3 หน้า | **จัดชั้นถูกอยู่แล้ว** — `pulse.json` คือ *หลักฐาน* (สัญญา §11.1) ไม่ใช่ของแสดงผล |

**สามบรรทัดที่เคยอยู่ตรงนี้ผิด และแก้แล้ว 8 ส.ค.:**

- ~~"workflow ยังไม่มี"~~ → **มี** · ผมวัดแค่ `nextStep` แล้วสรุปเกินหลักฐาน ·
  `nextStep` ไม่อ่าน workflow จริง แต่นั่นคือโดเมน **team** คนละเรื่องกัน
- ~~"runner พึ่ง display"~~ → **ไม่จริง** · สัญญาจัด `pulse.json` ไว้ฝั่ง EVIDENCE
  และเกตมีเหตุผลบันทึกไว้ใน `e0347a7` (watcher ตาย = หลักฐานค้าง = จ่ายรันซ้ำ)
- ~~"ฟิลด์แปลกปลอมบน route ถูกทิ้งเงียบ"~~ → **แก้แล้ว `24ca2f3`** · ทั้ง workflow และ
  team ชั้นบนปฏิเสธคีย์ที่ไม่ได้อ่าน พร้อมบอกว่าประกาศอะไรได้

**บทเรียน:** อย่าวัดจุดเดียวแล้วสรุปทั้งโดเมน · สามข้อข้างบนตายด้วยการวัดภายในชั่วโมงเดียว

## หลังรีลีส — 2 คอมมิตที่ยังไม่ push

- **`24ca2f3`** — workflow/team ที่ประกาศคีย์ซึ่ง loader ไม่อ่าน **ถูกปฏิเสธ ไม่ใช่ทิ้งเงียบ**
  · GitHub #47 เคยอุดให้ `seats` กับ palette แล้วหยุด ด่านเข้มสุดเลยไปนั่งในด่านหลวมสุด
  · ตรวจก่อนลงมือว่าไม่มี fixture/ผู้เรียกไหนพัง (82 จุด 18 ไฟล์) · AC119
- **`fb52747`** — ลูป**ตื่นเมื่อไฟล์เปลี่ยน** ใต้ `.mailbox-out/` กับ `work-items/`
  · **interval ยังอยู่** เพราะเป็นผู้อ่านเดียวของเส้นตาย 5 ตัว · อีเวนต์ที่พลาด =
  ช้าลง ไม่ใช่ผิด · พิสูจน์เส้นทางจริงแล้ว **ตื่นที่ 463ms แทน 30,000ms** ·
  repo ใหม่ที่ยังไม่มีโฟลเดอร์ **เสื่อมลงสะอาด ไม่พัง** (ENOENT → note) · AC120

## ที่ตัดสินแล้ว ห้ามรื้อ

- **D1** ขา audit ที่ตายที่ transport **ค้างไว้** ไม่มีอะไร retry เอง คนมาปลด
- **D2** การกู้คือ **คำถาม** (`questioned` → `answered`) **ไม่ใช่คำใหม่**
- **D3** งานที่เสร็จแล้วรอตรวจ = ของในคิวที่ทีม control ถือ (**กลไกยังไม่ถูกต้อง ดูข้างบน**)
- **D4** เพดานทั้งกระดานเป็นของ scheduler — **ข้อจำกัดของเครื่อง ไม่ใช่กฎเรื่องงาน**
- **D5** **หัวคิวกินช่อง ที่เหลือต่อแถว** — คิว ≠ ช่องทำงาน ถ้ารวมกันระบบค้างตั้งแต่ใบแรก
- **D6** กราฟไม่มีทีม control **ปฏิเสธตั้งแต่โหลด** (ยังไม่สร้าง)
- **MQ/event-driven ยังไม่ทำตอนนี้** — โดเมนยังไม่เป็นเจ้าของพฤติกรรมตัวเอง ทำก่อน = ทำสองรอบ
- **งานคำศัพท์ไม่ใช่โปรเจกต์แยก** — คำย้ายไปพร้อมโดเมนของมัน (Master ตัดสิน)

## DO NOT — ทำไปแล้วผิดวันนี้ อย่าทำซ้ำ

1. **`grep -c` นับบรรทัด ไม่ใช่จำนวนครั้ง** — ใช้ตรวจ stamp ซ้ำแล้วรายงานผิดทุกครั้ง
   เพราะ stamp สองอันอยู่บรรทัดเดียว · ใช้ `s.count()` แทน
2. **อย่าใช้ไดเรกทอรีเดิมข้ามเลน ACP** — receipt ไม่แยกตามเลน เลนที่สองจะพยายาม resume
   session ของเลนแรกแล้วตายทันที · **ไดเรกทอรีใหม่ทุกเลน**
3. **`liveness/*.json` เก็บ session id ไม่ครบ** (20 ตัว) — id เต็ม 36 ตัวอยู่ใน stdout ของ
   companion · **resume ด้วย id จากไฟล์นั้นล้มเสมอ** และได้ข้อความ `Resource not found`
   ที่ฟังเหมือน session หมดอายุ
4. **อ้างเลขบรรทัดที่วัดไว้ก่อนแก้ไฟล์** — แก้ `loop-runner.mjs` เองตอนเช้าแล้วอ้างเลขเก่า
   ทั้งวัน · เลขคลาดไป ~18 บรรทัด · **วัดใหม่ทุกครั้งหลังแตะไฟล์**
5. **เติมกล่องแก้แทนที่จะแก้ประโยคที่ผิด** — เขียนกล่อง "ประโยคข้างล่างเกินจริง" แล้วปล่อย
   ประโยคผิดไว้ · รีวิวจับได้ · **แก้ประโยค อย่าล้อมรั้ว**
6. **เขียนเทสที่ผ่านเพราะไม่ได้ตรวจอะไร** — `|| true` ใน UAT · และ crash test ที่สั่งให้ตาย
   ตอนไม่มีงานในมือ รายงาน 0 หายทั้งสองฝั่ง

## เลนรีวิว — สถานะจริง

| เลน | สถานะ |
|---|---|
| **codex luna** | โควตาหมดถึง ก.ย. |
| **zai** (`glm-5.2`) | ใช้ได้ · เคยได้รีวิว 25.9KB วันนี้ · `no_outbox` 2 ครั้งหลัง |
| **AGY** | **ใช้ได้ ถ้าไม่ระบุโมเดล** — รันค่าปริยายซึ่งคือ `gemini-3.6-flash-high` |
| **Claude Fable (subagent)** | **ใช้ได้** — ไม่ผ่าน ACP จึงไม่มีใบรับรองตัวตน |
| **tmux (ทุกโมเดล)** | **ใช้ได้ — เป็นทางสำรองเมื่อ ACP ล้ม** ดูกฎข้างล่าง |

**ลำดับการสั่งงาน — Master, 2026-08-07:** **ACP ก่อน · tmux เป็นทางสำรอง** · tmux ขับได้ทุก
โมเดล ฉะนั้นเลน ACP ที่ล้มย้ายบรีฟเดิมมา tmux ได้เลย **แต่ tmux จุกจิกกว่า** — เจอครบสามข้อ
ในการลองครั้งเดียว: trust dialog กินการ dispatch · Enter ต้องแยกคำสั่ง · ช่องพิมพ์ว่างโดยไม่มี
สัญญาณทำงานตีความไม่ได้ (SKILL.md บอกให้ถือว่ายังไม่ส่งหลัง 6 วิ แต่โมเดลช้าใช้เวลานานกว่านั้น
· รออีก 8 วิแล้วมันขึ้นเอง กฎตามตัวอักษรจะทำให้ส่งซ้ำ) · **และข้อที่ SKILL.md ไม่ได้เขียน:
`tmux capture-pane -p` ผ่าน pipe คืนค่าว่าง ต้อง redirect ลงไฟล์แล้วอ่านไฟล์**
· **ห้ามวางบรีฟยาวลง send-keys** — เขียนลงไฟล์แล้วส่งพรอมต์สั้นชี้ไปหา

**เรื่อง AGY ปิดแล้ว (2026-08-07):** adapter **ปฏิเสธค่าโมเดลทุกรูปแบบ** — ทั้ง
`gemini-3.6-flash-high` และสตริงที่มันรายงานเอง `"gemini-3.6-flash-high Gemini 3.6 Flash
(High)"` · **ไม่ส่งเลยถึงจะรันได้** และรันด้วยโมเดลที่ถูกต้องอยู่แล้ว · แต่ `matched`
เกิดไม่ได้เพราะการจะตรงกันต้องมีการขอก่อน · **Master ตัดสิน: ยกเว้น `identity_status`
ให้ AGY เลนเดียว ฟิลด์เดียว แต่ต้องจด `effective_identity` ที่รันจริงทุกครั้ง** — กฎเขียนไว้
ใน `CLAUDE.md` §Release flow ข้อ 2 พร้อมขอบเขต · **อย่าเอาไปอ้างกับเลนอื่น**

**`no_outbox` ไม่ใช่ของหายาก** — วันนี้ ACP สำเร็จ 5 ล้ม 6 · resume กู้ได้ 1 ใน 3
· สมมติฐานที่ตายไปแล้ว: ขนาดบรีฟ · ตาราง/โค้ดบล็อก · เลนใดเลนหนึ่ง · เครื่องแน่น (load 2.6)
· **คำเตือน `lacks requested prior lineage` ไม่ทำนายความล้มเหลว** — resume ที่สำเร็จก็มี

## อิชชูผูกกับโดเมน — 2026-08-07

**แผนรื้อโดเมนไม่ใช่งานคนละกองกับ backlog มันคือกองเดียวกันเรียกคนละชื่อ**
ไม่มีใครเขียนผูกไว้ ความคืบหน้าจึงวัดไม่ได้ และดูเหมือนเลิกทำ backlog

| อิชชู | เรื่อง | โดเมน |
|---|---|---|
| **#54** | `pm.md` ทับ PM brief ทั้งใบ ไม่มี trigger/board/verdict | **team** — ทีมประกาศวิธีทำงานเองไม่ได้ |
| **#56** | resume ส่งกลับได้แค่ทีมที่ escalate มา สองทีมตีกันไม่มีทางออก | **workflow** — เส้นทางไม่มีเจ้าของ |
| **#50** | โทเค็นถอนที่ประตูค้างใน PM trigger set ตลอดกาล | **token** — ช่อง WIP ของ PM |
| **#52** | audit ตายเพราะ 429 ถูกนับเป็น "ไม่มีใครมา" | **token** — ช่อง WIP ของ PM |

**#56 ขอของที่หน้าตาเหมือน `answer.mjs` ตรงๆ:**
`override.mjs --repo <repo> --work-item <token> --team <team> --reason <text>`
· workaround ที่บันทึกไว้ในอิชชูคือ **คนเขียน JSON ลงสมุดด้วยมือ** พร้อมไปอ่าน field spec
ของ validator เอง · **และอิชชูเดียวกันยกคอมเมนต์ในโค้ดเราเองมาชม** ว่า
*"ask the person instead"* คือแบบที่ถูก — โดยไม่รู้ว่าตอนนั้นไม่มีใครตอบได้เลย

## ถัดไป ตามลำดับ — Master สั่ง 2026-08-07 ให้ทำให้จบทั้งชุด

**ห้าข้อแรกเสร็จหมดแล้ว · suite 773 fail 0 · ทั้งหมดยังไม่ push**

| คอมมิต | เรื่อง |
|---|---|
| `9fecd40` | `answer.mjs` — ลูปถามได้ แต่ไม่มีใครตอบได้ |
| `13b404c` | retry ของ audit มีอยู่เพราะไม่มีใครตอบได้เท่านั้น · ปิด #52 |
| `56d00e9` | คำถามที่ไม่มีที่นั่ง หยุดอะไรไม่ได้ อย่าเขียน |
| `7dab151` | **D6** ทีม control บังคับ · 212 เทสใน 17 ไฟล์ · แตะ #50 |
| `83b93a0` | `withdraw.mjs` — คนปิดโทเค็นได้ · แทนที่ #56 |

**สามประตูของคนครบแล้ว:** เปิด (`admit.mjs`) · ตอบ (`answer.mjs`) · ปิด (`withdraw.mjs`)

### ที่ตัดสินระหว่างทาง — อย่ารื้อ

- **D6 ทำเลย ยอมแก้ 212 เทส** (Master 2026-08-08) · เทส 5 ตัวที่ตรึงโลกเก่า **เขียนใหม่ ไม่ลบ**
- **#56 เอาทาง B ไม่เอา A** (Master 2026-08-08) · **ไม่ทำ `override.mjs`** — การส่งงานข้ามทีม
  ย้อนทางไหล · ทำ `withdraw.mjs` แทน แล้วเปิดใบใหม่ ตรงกับที่ §6.3 เขียนไว้เอง
- **กฎวางตำแหน่งแตกเป็นสองชุด** — `PLACES_BY_DESTINATION = {resumed}` เท่านั้น ·
  `escalated` **ไม่อยู่ในชุดนี้โดยตั้งใจ** เพราะ escalate คืองานของ PM

## ถัดไป

1. **ปล่อยรีลีส** — เกตสามโมเดลอ่าน diff จริง (`git diff 8ab1016..HEAD`) ก่อนติดเลขเวอร์ชัน
   · แล้วบัมพ์ 4 ที่ + `node --test` + tag + release + marketplace
2. **commit `CLAUDE.md` กับ `docs/`** — ทั้งคู่ยังค้าง (`docs/` ไม่อยู่ในรายการ tracked)
3. **เอกสารแผนพัฒนา** อยู่ที่ `https://artifacts.ngs.bz/claude/private/tmux-teams-release-plan/`
   · ไฟล์ทำงาน `docs/artifacts/claude/private/tmux-teams-release-plan/index.html`
   · **ต้องมี `<meta charset="utf-8">` เสมอ** — สคริปต์ artifact-sftp ไม่ห่อ head ให้ ภาษาไทยจะเพี้ยนทั้งหน้า
4. **ตัดสินเรื่อง AGY** · 6 อิชชูที่เหลือยังไม่แตะ (#49 #51 #53 #55 #57 #58 #59)

## หนี้ที่รู้ตัว

- **`docs/` ยังไม่ commit** — เป็น path ที่ไม่อยู่ในรายการ tracked ของ repo นี้
- **`plan5` ยังไม่มีเลน ACP ไหนอ่านจบ** — ที่ได้มาคือ Fable ผ่าน subagent
- **submodule pin ใน `~/agent-skills`** ยังไม่ push · คนละโปรเจกต์ ต้องขอ Master
- **29 worktree ค้าง** ใน `.claude/worktrees/` — หนี้พื้นที่ ไม่ใช่หนี้ความถูกต้อง

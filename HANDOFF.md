# HANDOFF — tmux-teams · หลังปล่อย v0.14.2 (2026-08-03)

## อ่านสิบวินาทีแรก

- **v0.14.1 ปล่อยครบทุกช่องทางแล้ว** — GitHub `76f490d..0c564ac` (8 commit),
  marketplace อัปเดต, plugin 0.14.0 → 0.14.1, submodule ใน `~/agent-skills`
  pin ที่ `fa1a96b`
- ⚠️ **ต้อง restart** ให้ปลั๊กอิน 0.14.1 มีผล
- **งานค้างทั้งหกข้อของ v0.14.0 ปิดครบแล้ว** · เจองานใหม่ 3 ข้อระหว่างทาง อยู่ท้ายไฟล์
- ~~`main` สะอาด · **484/484**~~ — **คำอ้างนี้ผิด** · 2026-08-03 รันจริงบนเครื่อง Master
  ได้ **489/494 แดง 5** ใน `tests/acp-companion.test.mjs` และมันแดงมาตั้งแต่ `f528168`
  (27 ก.ค.) ผ่านมาสามรีลีส · แก้แล้ว ดูท้ายไฟล์ · ตอนนี้ **494/494** ของจริง
- **ปาร์ตี้โหมดถูกบังคับด้วย hook** — `.claude/hooks/party-gate.mjs` บล็อกคำตอบที่
  ไม่มีเสียงจากคณะจริง และบังคับให้มี **John (PM)** กับ **Sally (UX)** ทุกคำตอบ
  · คณะมาจาก `resolve_party.py` ห้ามกุชื่อ

## v0.14.0 คืออะไร

รีลีสที่ **ลบ** — net **−12,331 บรรทัด** (+1,960 / −14,291)

| | |
|---|---|
| ลบระบบเฟสสี่ขั้นทั้งก้อน | 9 สคริปต์ ~5,666 บรรทัด + เทสต์ + 5 schema + runbook + แถบบนหน้า Pulse |
| ถอนคำสั่งที่เคยประกาศ | `delivery-loop-export.mjs export` / `verify-pack` · แฟล็ก `--delivery-loop` / `--delivery-runtime` |
| แก้ ACP trust boundary 7 ข้อ | ทุกข้อมี negative control พิสูจน์ด้วย mutation |
| เขียนคู่มือ + ผังงานใหม่ | `README.md`, `references/how-it-works.md` (6 mermaid) |
| แก้หน้ากราฟตามที่ Master สั่ง | แถบเดียว 33px · canvas 78vh · ปุ่ม `full` · เส้นภายในทีมเป็นประ · reject อ้อม worker |

**ข้อที่กระทบผู้ใช้จริงที่สุด:** เดิม `modelEnv()` ส่งแค่ `ACP_EXPECT_MODEL` ซึ่งเป็น
*ข้อเรียกร้อง* ไม่ใช่ *คำขอ* — **ทุกที่นั่งวิ่งบน account default มาตลอด** ทั้งที่กราฟ
ประกาศโมเดลไว้ และที่นั่งที่ประกาศอย่างอื่นจะ fail · ตอนนี้ส่ง `ACP_MODEL` ด้วย และ
ที่นั่งเลือก lane เองได้ (`adapters` ต่อทีม, `outer_controller_adapter`)

## v0.14.1 คืออะไร — งานค้างทั้งหกของ v0.14.0 ปิดครบ

> เก็บรายการไว้ทั้งหมดพร้อมสิ่งที่พบระหว่างทาง เพราะหลายข้อกลายเป็นงานมากกว่าที่จดไว้
> · 8 commit · **+734 / −61** · 494/494

1. ~~**สอง SSOT**~~ — **ปิดแล้ว 2026-08-03** · `loop-graph-page.md` เลิกประกาศตัวเป็น
   SSOT และประกาศว่าเป็น **บทขยายของสัญญา §12** พร้อมกติกาว่า**สัญญาชนะเมื่อขัดกัน** ·
   สัญญามีสารบัญ `Where to look` §0–§15 ที่หัวไฟล์แล้ว และ §12 ชี้ลงไปที่ไฟล์รอง ·
   คำอ้าง SSOT ก้อนที่สามที่ `graph-tour.mjs:3` ถูกแก้ด้วย และ `CLAUDE.md` เลิกย้าย
   คำอ้างเข้ามาไว้ในไฟล์ที่ทุกเซสชันโหลด · **ยังชี้ §9 เหมือนเดิม ห้ามเปลี่ยนเลข §**
2. ~~**`pulse.mjs` ไม่ปฏิเสธแฟล็กที่ไม่รู้จัก**~~ — **ปิดแล้ว 2026-08-03** · ทุก argument
   ต้องเป็นตัวที่**คำสั่งนั้นอ่านจริง** ไม่งั้น exit 2 พร้อมบอกชื่อ argument ·
   `--interval`/`--managed` ถูกปฏิเสธบน `once`/`json` ที่ไม่เคย watch ·
   `--interval abc|0|-5` ถูกปฏิเสธแทนที่จะตกไปใช้ค่า 20 เงียบ ๆ ·
   เทสต์ 2 ตัวใน `pulse.test.mjs` ผ่าน mutation แล้ว (ถอด guard → แดงทั้งคู่)
3. ~~**กฎ flow ทางเดียวยังไม่มีอะไรบังคับ**~~ — **บังคับแล้ว 2026-08-03** ·
   `validateLedger` ปฏิเสธ `pulled` ที่ `to_team` เป็นทีมซึ่ง**รับเข้า** token นี้ไปแล้ว
   (code `route_went_backwards`) และเพราะ writer ตรวจทั้งก่อนและหลังทุก append
   บรรทัดนั้นจึงไม่เคยลง · **นับตอน `intake` ไม่ใช่ตอน `pulled`** — ทีมที่ปฏิเสธที่ประตู
   ไม่เคยรับเข้า จึงยังดึงงานเดิมได้อีกหลังแก้ · `loop-replay.test.mjs` seed 1 เป็นตัวพิสูจน์
   ข้อนี้ตอนที่เขียนผิดรอบแรก · negative control 2 แบบใน `ledger.test.mjs`
   · **มติ Master 2026-08-03 สองข้อ ทำแล้วทั้งคู่:** (ก) ledger เก่าที่มี backwards pull
   ยัง**เขียน terminal event ปิดได้** แต่เขียนอย่างอื่นไม่ได้ และข้อยกเว้นกว้างแค่ code เดียว
   (ข) **ประตูปฏิเสธได้ 3 ครั้งต่อทีม** ครั้งที่ 4 runner เขียน `escalated` แทน และ
   validator ปฏิเสธด้วย `door_refusals_exhausted` · mutation 4 แบบแดงครบ
   · **ยังเหลือ:** `pull-controller.mjs` ทิ้งค่า refusal ลง stderr — การปฏิเสธของ writer
   จึงมองไม่เห็นจาก plan (นี่คือสิ่งที่ทำให้ token ค้างเงียบตอนดีบัก)
4. ~~**เทสต์ที่เขียวแต่ไร้ความหมาย**~~ — **ตรวจแล้ว 2026-08-03 ไม่ได้ไร้ความหมาย** ·
   probe จริง: publish เปล่า = `complete`, ตัวที่อัปเกรด = `degraded` · แต่มันยืนยัน
   **ข้างเดียว** จึงเติมอีกครึ่งเข้าไป (`next.observation.quality === 'complete'`) —
   ถ้าอนาคตมีอะไรทำให้ทุก publish degraded เทสต์จะแดงแทนที่จะเขียวเงียบ ·
   **ช่องว่างของ Gate 1 ยังอยู่** — การเทียบชื่อเทสต์มองไม่เห็นเทสต์ที่ชื่อเดิมแต่เลิกตรวจ
   วิธีกันคือยืนยันสองข้างเสมอ
5. ~~**`title` เข้าถึงได้เฉพาะเมาส์**~~ — **แก้แล้ว 2026-08-03** · ข้อเท็จจริงบนแถบบน
   (จำนวนทีม/เวิร์กโฟลว์, path ของ graph, `snapshot_id`, เวลาที่สังเกต, สถานะ runner)
   อยู่ใน `<details>` แล้ว — คีย์บอร์ด/แตะจอ/screen reader เข้าถึงได้ `title` ยังอยู่เป็น
   ทางลัดของเมาส์ · **วัดในเบราว์เซอร์จริง** แล้วเจอสามอย่างที่เทสต์ HTML มองไม่เห็น:
   panel กว้าง 123px เพราะ abspos shrink-to-fit (แก้ด้วย `width:max-content`),
   panel ทับแถบเมื่อบาร์ตัดบรรทัด (ยึดกับ `.topbar` แทน `.repo`) และ `pulse-refresh`
   จำสถานะ `<details>` **ด้วยลำดับ** ถ้าไม่ตั้งชื่อ (ใส่ `data-refresh-details-key`) ·
   **เจอเพิ่ม ยังไม่แก้:** tooltip ของการ์ดใน `kanban.mjs:344` มีสองอย่างที่ไม่ปรากฏบนการ์ด
   — `work_item` เต็ม (การ์ดตัดที่ 60) และเวลาสัมบูรณ์ของ event ล่าสุด
6. ~~**guard backtick เป็น allowlist สามก้อน**~~ — **แก้แล้ว 2026-08-03** ·
   `plugin-structure.test.mjs` เดินทั้งต้นไม้ที่ ship: (ก) `node --check` ทุก `.mjs`
   — ที่ CLAUDE.md เคยขอให้ *คน* จำว่าต้องรัน ตอนนี้ชุดเทสต์จำแทน (ข) import ทุกโมดูล
   ที่ไม่ใช่ CLI แล้วตรวจ export ทุกตัวที่ชื่อลงท้าย `CSS`/`SCRIPT` ว่าไม่มี backtick ·
   มี floor กันการเดินที่ไม่เจออะไรแล้วผ่านเงียบ · **พิสูจน์ด้วย mutation ที่ `NAV_CSS`
   ซึ่ง allowlist เดิมไม่เคยครอบ** — escaped backtick ที่ยัง parse ผ่านก็ยังโดนจับ

## มติที่ตัดสินไปแล้ว — อย่ารื้อ

- **worker ต่อทีมไม่เกิน 5** — เหตุผลของ Master: `1, 2, 3, 5, 8, 13` — 3 น้อยไป 8 มากไป
  · **fail closed** ที่ `workflow-graph.mjs` ตัวที่ 6 ถูกปฏิเสธ
- **flow ไม่ย้อนข้ามทีม** · แต่ **evaluator วนงานกลับให้ worker ในทีมเดียวกันได้**
  และ **dispatcher ปฏิเสธที่ประตูได้** (ตรวจก่อนรับเข้า ไม่ใช่รับเข้ามาแล้วส่งกลับ)
  · ตัวอย่างของ Master: QA evaluator เจอ checklist ไม่ครบ → ส่งกลับ worker ตัวเอง ·
  เจอบั๊กจริง → **QA แก้เอง ไม่ส่งกลับ Dev**
- **หน้าแรกของกราฟ = สถานะ ไม่ใช่ประวัติ** — เส้นภายในทีม (`assign`/`judge`/`reject`/
  `owns`) เป็นเส้นประเสมอ ไม่แข็งตัวตามหลักฐาน · **เส้นทึบอื่นคงไว้** (`pull`,
  `passed`, route)
- **guard ของ phase-gate ถูกลบพร้อมระบบ** (codex-advisor ตัดสิน) เก็บแค่ tombstone:
  repo ที่ยังมี `.tmux-teams/phase-gate.json` **ถูกปฏิเสธ** ไม่ใช่ลดชั้นเงียบ

## วิธีตรวจที่พิสูจน์แล้วว่าใช้ได้ — ใช้ซ้ำ

**เทสต์เขียวไม่ใช่หลักฐาน** เมื่อของที่ถูกลบพาเทสต์ของตัวเองไปด้วย

1. **เก็บชื่อเทสต์ก่อน + ประกาศ allowlist ล่วงหน้า** ว่าชื่อไหนอนุญาตให้หาย ·
   หลังลบ `before − after` ต้องเท่ากับ allowlist เป๊ะ (ผลจริง: หาย 67 นอกรายการ 0)
2. **canonical witness** — `baseline-probe.mjs` รัน loop จริงบน fixture สองทีม
   `Build → Verify` แล้วคายภาพ ledger/board/activity/outbox/pulse ·
   **map `task_id`/`dispatch_id` แบบ bijection ห้ามลบทิ้ง** — ความสัมพันธ์ของ ID
   คือหลักฐานว่าพูดถึง leg เดียวกัน · เสถียร 3 รอบก่อน ไบต์เหมือนเดิมหลังลบ
3. **mutation test ทุก guard** — ถอด guard แล้วเทสต์ต้องแดง · วันนี้ทำ 11 ครั้ง
4. **หน้าเว็บวัดใน browser เท่านั้น** — `getComputedStyle`, `getBoundingClientRect`,
   นับ `animateMotion` · **ห้ามรายงานจากภาพนิ่ง** (`loop-graph-page.md` §9)

## กับดักที่เสียเวลาไปจริงวันนี้

- **backtick ในคอมเมนต์ปิด template literal** — `TOUR_CSS`/`TOUR_SCRIPT` ·
  เกิดครั้งที่ 8, 9, 10 ของโปรเจกต์ วันนี้วันเดียว · `node --check` จับได้ทุกครั้ง
  **รันมันก่อนรันเทสต์**
- **python `str.replace` ที่ไม่ assert ก่อนเขียน** — เขียนไฟล์เหมือนเดิมแล้วเงียบ ·
  ใช้ `assert old in s` ทุกครั้ง
- **ขยายกฎที่กู้มาโดยไม่รัน** — ขยาย schema invariant 3 ครั้ง ผิดทั้ง 3 ครั้ง
  เพราะ v3/v4 ตั้งใจให้ต่างจาก v2 · **การ scope แคบของต้นฉบับมักถูกแล้ว**
- **process ที่ pin โค้ดเก่า** — `pulse watch`, `loop-runner`, plugin cache
- **`git status` ที่ดูสะอาดหลอก** — `.tmux-teams/` ignore ตัวเองด้วย `.gitignore`
  ข้างใน (เขียนอธิบายไว้ในไฟล์รากแล้ว)

## ความเห็นที่วงยังไม่ตรงกัน — อย่ากลบ

- **สอง SSOT vs แฟล็กเงียบ** ข้อไหนแพงกว่า — Winston: สร้างของผิดแพงกว่า ·
  Murat: สคริปต์ที่พังเงียบกระทบคนวันนี้
- **สเปกยาว 1,209 บรรทัด** — Winston: ระบบเงื่อนไขเยอะต้องยาว · Sally: ยาวจนไม่มี
  ใครอ่านจบ ต้องมีหน้าเดียวที่ชี้ทาง
- **page fingerprint เป็น release blocker ไหม** — Murat: ใช่ หน้าคือของที่ ship ·
  John: เกิน minimum ถ้า semantic snapshot ผ่าน

## ที่อยู่ของของสำคัญ

```
แผนที่อนุมัติแล้ว     ~/.claude/plans/jiggly-spinning-anchor.md
สัญญาระบบ             plugins/tmux-teams/skills/tmux-teams/references/loop-system-contract.md
ผังงาน 6 ภาพ          plugins/tmux-teams/skills/tmux-teams/references/how-it-works.md
กฎหน้ากราฟ + วิธีตรวจ  plugins/tmux-teams/skills/tmux-teams/references/loop-graph-page.md §9
hook บังคับปาร์ตี้      .claude/hooks/party-gate.mjs  (gitignored, machine-local)
probe เก็บพยาน        scratchpad ของเซสชันเก่า — เขียนใหม่ได้จากสูตรในแผน P5
```

## งานใหม่ที่เจอระหว่างทาง — ยังไม่ทำ

1. ~~**`pull-controller.mjs` ทิ้งค่าที่ writer คืนมา**~~ — **แก้แล้ว 2026-08-03**
   (`36571a9`) · และมันหนักกว่าที่จดไว้: runner ไม่ได้แค่มองไม่เห็น refusal มัน
   **พิมพ์ `pull x: build -> test` ออกมาเหมือนเขียนสำเร็จ** ซึ่งสัญญา §4.2 ห้ามตรง ๆ ·
   ตอนนี้ผลเดินทางกลับมากับ decision (`write_result`), runner รายงานผ่าน `log()`
   ของตัวเอง, และบรรทัดสำเร็จถูก gate ไว้ · เลขบรรทัดใน P4 เพี้ยน — ตัวจริงคือ
   `loop-runner.mjs:1089` ไม่ใช่ `:1014` · mutation 3 แบบแดงครบ
2. **tooltip การ์ดใน kanban** (`kanban.mjs:344`) มีสองอย่างที่ไม่อยู่บนการ์ด —
   `work_item` เต็ม (การ์ดตัดที่ 60) และเวลาสัมบูรณ์ของ event ล่าสุด · คลาสเดียวกับข้อ 5
3. **หน้ากราฟเลื่อนแนวนอนที่ 400px** — `scrollWidth` 538 ทั้งเปิดและปิด panel
   จึงเป็นบอร์ด ไม่ใช่ของใหม่ · มีอยู่ก่อนแล้ว

## GitHub

- **issue #21 — ปิดแล้วเปิดใหม่ในวันเดียวกัน** · ข้อ 1 (lane hardcode) และข้อ 2
  (identity mismatch) แก้จริงและปล่อยแล้ว · **ข้อ 3 ผมตอบเกินหลักฐานแล้วปิดไป** ·
  ที่ลบใน v0.14.0 คือ **gate ที่ปกครองเฟส** ไม่ใช่ **คำศัพท์** ที่ issue บ่นถึง ·
  บน 0.14.1 `acp-companion.mjs:189-191` ยังปฏิเสธทุกค่าที่ไม่ใช่
  `Requirement|Prototype|Development|QA` และ `pulse.mjs:51-56,176` ยังถือ
  `PHASE_BOUNDARIES`/`DELIVERY_PHASE_SET` ผูกกับ `phase:` ในทุก dispatch footprint ·
  **บทเรียน: ตรวจไบต์ก่อนปิด ไม่ใช่หลังปิด**
- **ถอน `TMUX_TEAMS_PHASE` เป็นมติแยกที่ยังไม่ได้ตัดสิน** — แผน P5 เขียนไว้แล้วว่า
  ต้องเป็นข้อแยก เพราะแตะทั้ง footprint ที่เขียนไปแล้วและ schema ของ Pulse ·
  `tests/pulse-v3-phase.test.mjs` เป็นตัววัดที่รอดข้ามการลบรอบก่อนมาได้

## 2026-08-03 (รอบบ่าย) — เทสต์แดง 5 ตัวที่ไม่มีใครเห็นมาเจ็ดวัน

**อาการ:** ชุดเต็มบนเครื่อง Master ได้ 489/494 แดง 5 ทั้งหมดอยู่ใน
`tests/acp-companion.test.mjs` เรื่อง cancel/reap · ยืนยันว่า**ไม่ได้เกิดจากงานวันนี้**
ด้วย `git stash` แล้วรันบน main สะอาด ได้ชื่อเดิมทั้งห้า และรันนอก sandbox ก็เท่าเดิม

**bisect ชี้ commit เดียว:** `f528168^` (25 ก.ค.) เขียว → `f528168` (27 ก.ค.) แดง ·
commit นั้นเพิ่ม**ทั้งฟีเจอร์ `descendant_cleanup_signal_delivered` และเทสต์ที่ตรวจมัน
พร้อมกัน** → มันแดงตั้งแต่วันแรกที่เกิด ผ่าน v0.13.x, v0.14.0, v0.14.1 มาครบ

**ต้นเหตุ — สองบรรทัดใน `acp-companion.mjs`:**

```js
groupPids(groupId)        if (!groupId || process.platform !== 'linux') return []
waitForGroupGone(ms)      if (process.platform !== 'linux') return true
```

ทั้งคู่**ตอบโดยไม่ดู** — ตัวหนึ่งบอกว่า "ไม่มีใครอยู่ในกลุ่มนี้" อีกตัวบอกว่า "กลุ่มตายแล้ว" ·
บน macOS การกวาด descendant ทั้งสองจุด (`:2648` หลัง child settle และ `:2652`
กวาดกลุ่มที่เหลือ) จึงเป็น no-op **ทุก dispatch ที่ถูก cancel ทิ้ง orphan process ไว้**

**ทำไมไม่มีใครเห็น** สองชั้น
1. ชุดเทสต์เขียวบน Linux — CI กับเครื่องที่ใช้งานจริงไม่ใช่เรื่องเดียวกัน
2. เทสต์ตรวจ**ถ้อยคำในใบเสร็จก่อน**ตรวจว่า process ตายจริง → orphan ที่ยังวิ่งอยู่
   โผล่มาเป็น "regex ไม่ตรง" · assertion ที่แดงก่อนบัง assertion ที่สำคัญกว่าไว้ข้างหลัง

**แก้แล้ว:** `groupPids` มีทาง macOS/BSD ผ่าน `ps -Ao pid=,pgid=` (แบบเดียวกับที่
`pulse.mjs:226` ใช้ `DARWIN` อยู่แล้ว — โรคนี้เคยรักษาไปแล้วในไฟล์ข้าง ๆ) ·
`waitForGroupGone` เลิก early-return แล้วใช้ `kill(-pgid, 0)` ซึ่งเป็น POSIX ·
เทสต์สลับลำดับให้ `assertPidGone` มาก่อน · **130/130 · ชุดเต็ม 494/494**

**หมายเหตุเรื่อง mutation:** ถอด guard ทีละตัว**ยังเขียว** เพราะทั้งสองเป็นทางไปสู่
ผลลัพธ์เดียวกันคนละเส้น ถอดพร้อมกันถึงแดง (`cancel-ack pid N should be reaped`) ·
เทสต์ตัวนี้ผูกกับ**ผลลัพธ์** ไม่ใช่กลไก และนั่นถูกแล้ว

**กฎที่ได้มา สองข้อ**
- เทสต์ต้องยืนยัน**ผลจริงก่อนถ้อยคำที่บรรยายผล** เสมอ
- กิ่ง platform ที่ตอบไม่ได้ต้องบอกว่า **UNKNOWN ห้ามบอกว่า "ไม่มี"** — `return []`
  กับ `return true` ที่แปลว่า "ไม่รู้" คือสิ่งที่ทำให้บั๊กนี้เงียบมาเจ็ดวัน

## v0.14.2 — ปล่อยแล้ว 2026-08-03

3 commit ทั้งหมดเป็นตระกูลเดียวกัน: **มีอะไรบางอย่างหยุด แล้วไม่มีอะไรบอก**

| commit | อะไร |
|---|---|
| `36571a9` | refused pull ไม่ถูกรายงานว่าสำเร็จอีก (ละเมิดสัญญา §4.2) · mutation 3 แบบ |
| `e053d89` | `watch --managed` บอกคำสั่งที่ต้องใช้ แทน "belongs to pid none" |
| `e8c795e` | **orphan process ไม่ค้างอีก** บนเครื่องที่ไม่มี `/proc` · เทสต์สลับลำดับ |

**บทเรียนเรื่องบัญชี GitHub — เสียเวลาไปหนึ่งรอบเต็ม:** `gh` บนเครื่องนี้มีสามบัญชี
และตัวที่ active คือ `ngs-th` ซึ่ง**อ่าน `iicmaster` ได้ เขียนไม่ได้** · `git push origin`
คืน 403 แต่ remote `fork` รับ push เข้าไปเรียบร้อย → เกือบจบวันโดยเชื่อว่า ship แล้ว
ทั้งที่ `origin/main` ไม่ขยับ · **ต้อง `gh auth switch --user iicmaster` ก่อน** และ
ตรวจว่า `git status -sb` ไม่มีคำว่า `ahead` ไม่ใช่แค่ดูว่า push พิมพ์ hash ออกมา ·
เขียนกฎนี้ลง `CLAUDE.md` แล้ว

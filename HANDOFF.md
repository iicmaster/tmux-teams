# HANDOFF — tmux-teams · หลัง v0.14.3 + งานรอบสอง (2026-08-03)

> **อ่านท้ายไฟล์ก่อน** — หัวข้อ "รอบสอง" คือสถานะปัจจุบัน · รายการค้างสี่ข้อของรอบแรก
> ปิดครบแล้ว · มี 4 commit ที่ยังไม่ bump version

## อ่านสิบวินาทีแรก

- **v0.14.1 ปล่อยครบทุกช่องทางแล้ว** · หลังจากนั้น **7 commit ยังไม่ push**
  (`4428b50..3d5b75c`) — ปิด GitHub #22–#26 ครบ บวกงานที่ Master สั่งระหว่างวัน
- ⚠️ **ชุดเทสต์เต็มยังไม่ได้รันหลัง `0b1cb72`** · เครื่องเดิมแบก `nuxt dev`,
  claude อีกสองเซสชัน, `agy`, `codex app-server` พร้อมกัน load แตะ **34**
  · **fast tier เขียว 147/147** ครอบทุกไฟล์ที่แตะยกเว้น ACP
  · **ถือเป็นหนี้ ไม่ใช่ผ่าน** — คำสั่งแรกบนเครื่องใหม่คือ `node --test`
- **ยังไม่ bump version** — ของทั้งหมดยังไม่ถึงผู้ใช้จนกว่าจะ bump + push

## วิธีรันเทสต์ — อ่านก่อนรันอะไร

```bash
node scripts/run-fast.mjs fast   # 2.3 วินาที · 147 เทสต์ · ใช้ระหว่างทำงาน
node --test                      # ~120 วินาที · กิน 8 คอร์เต็ม · ครั้งเดียวก่อน commit
```

ผมรันชุดเต็มไปสิบกว่ารอบวันนี้ทั้งที่ `CLAUDE.md` เขียนไว้แล้วว่าให้ใช้ fast tier
จน Master ต้องสั่งหยุดสามครั้งและคิดจะย้ายเครื่อง · **ต่างกัน 50 เท่า**

`grep` เหนือผลเทสต์ **ไม่ใช่ gate** — คืน 0 ตอน*เจอ* `✖` · ผม commit ทับแดง
ไปสองครั้งเพราะเรื่องนี้ · กติกาที่ถูกอยู่ใน `CLAUDE.md` แล้ว

---

## GitHub #22–#26 — ผลตรวจกับไบต์จริง

เปิดโดย `iicmaster` จากการขับงานจริงบน v0.14.0 พร้อม token id และ log ·
ทุกใบผ่านตัวตรวจสองชั้น (วิเคราะห์ → refute ที่ตั้งให้ default เป็น REFUTED
ถ้า file:line ไม่ตรง) · **ต้นเหตุยืนทั้งห้าใบ แต่กลไกที่ผู้รายงานระบุผิดสี่ใบ**
บันทึกไว้ที่นี่เพราะรายงานบน GitHub ยังเขียนกลไกที่ผิดอยู่

### #22 — controller ถูกปลุกทุก cooldown · `5701fb4`

| ผู้รายงานว่า | ของจริง |
|---|---|
| เบรก unchanged กันได้แค่ในช่วง cooldown | **กลับหัว** — `:903-913` cooldown `return` ก่อน เบรกทำงาน**หลัง** 900 วิ และกันได้ตลอด |
| `failedLegs` นับย้อนหลังทั้งหมดคือต้นเหตุ | จริงแต่**ไม่ใช่สาเหตุ** — ปิด stall trigger แล้วจำลอง 3 ชม. ได้ dispatch ครั้งเดียว |

**ตัวจริง:** `:883` ใส่ `Math.round(idleSec / 60)` ลงในข้อความ แล้วเบรกเทียบด้วย
**สตริง** → บอร์ดที่นิ่งทำให้ตัวเลขนาทีเพิ่มทุก tick → ไม่มีวันตรงซ้ำ
ตัวที่สอง `:686` พอ mark ลง token เป็น `held` หลุดจากชุด trigger → ชุดหด

**ทางแก้:** trigger แยก `text` (คนอ่าน) ออกจาก `id` (เครื่องเทียบ) · stall ใช้
**เวลาของเหตุการณ์สุดท้ายที่ถูกบันทึก** แทนอายุของมัน

**ทำไมหลุดมาได้:** `loop-occupancy.test.mjs:594` ส่ง `stallSec: 1e9` — **ปิด
trigger ตัวเดียวที่ทำลายเบรกได้** ส่วน production เรียกโดยไม่ส่ง option ·
**เทสต์ที่เฝ้ากฎนี้ไม่เคยรันคอนฟิกที่ ship สักครั้ง** · แก้แล้ว พร้อม assertion
ว่าสองรอบต้องเรนเดอร์ข้อความต่างกันจริง

**ห้ามทำ:** `if (!auditSubject && !parkedSubject) return null` — วัดแล้วว่าทำเทสต์
แดง 3 ตัวใน 2 ไฟล์ และกลับบทบาท controller ตาม §9:574-575 ที่ว่ามันถูก dispatch
*about the board, not about a token*

### #23 — review gate ไม่บอกว่าเลนไหนพัง · `0b1cb72`

ข้อมูลถูกเก็บครบอยู่แล้ว — `assessAttempt` ผลิต `failureKind`/`failure` ทุกเลน ·
บั๊กคือ **capture-then-discard**: `report` ถูกสร้าง**ใต้** throw ทั้งสิบจุด ·
hoist `fail()` ขึ้นเหนือ throw แล้ว `if (error?.report)` ที่ CLI มีอยู่แล้วก็ทำงาน

**วัดแล้ว:** kimi พัง 4 แบบต่างกันเชิงโครงสร้าง (`closed` ×2 ข้อความต่างกัน,
`timeout`, `spawn`) → เดิม output เหมือนกันทุกไบต์ · ตอนนี้ต่างกันทั้งสี่

**ห้ามทำ:** ใส่ `stderrTail` ดิบของ provider ลงรายงาน · `stderrDigest`/`stderrBytes`
มีอยู่แล้วเป็นคำตอบที่ไม่รั่ว · `redactString` มี 8 pattern ซึ่งไม่พอกับข้อความ
ที่ provider ควบคุม · เทสต์ยืนยันว่าความลับปลอมไม่โผล่

### #24 — adapter/model ผูกกับ role ไม่ใช่ seat · `3d5b75c`

**ตัวอย่างของผู้รายงานเอง ข้อเสนอของผู้รายงานแก้ไม่ได้** — `kimi` ไม่ใช่ adapter
(`ADAPTERS = {claude, codex, agy}`) มันวิ่งบนเลน claude ผ่าน `ANTHROPIC_BASE_URL`
ที่ `childEnv` ส่งต่อจาก env ของ runner → **เป็น process-global ทั้ง runner** ·
per-seat endpoint ประกาศไม่ได้ และ §3.2.1 เขียนไว้ตรง ๆ แล้ว

**ที่ ship:** `seats` map ต่อ agent_id override `model`/`adapter` ของที่นั่งเดียว ·
ทุกวิธี "ประกาศแล้วไม่ได้พูดอะไร" ถูกปฏิเสธ ไม่ใช่เมิน · 3 รูปแบบ (ไม่มี `seats`,
`seats: {}`, entry ที่ทวนค่าเดิม) ให้ `source_digest` เดียวกัน

**บั๊กจริงที่เจอระหว่างอ่าน:** `outer_controller_adapter` ถูก validate, normalize,
คืนจาก `declaredAdapter` แล้ว**ถูกทิ้งตอน dispatch** — กราฟที่ประกาศ `codex`
วาด codex บนหน้าเว็บ แล้ว spawn `claude` · log เขียน `model=` ไม่เขียน `lane=`
จึงไม่มีอะไรในบันทึกขัดแย้งได้

**ไม่ ship (คนละระบบ มี AC ของตัวเอง):** per-seat credentials · fanout + quorum
(อันหลังคือสิ่งที่แก้ §6 "the single placement rule")

### #25 — `watch --managed` ตอบ `pid none` · `ea730b0`

**อาการที่รายงานเป็นครึ่งเล็ก** · ครึ่งใหญ่คือ **ใครก็ตามที่อ่าน pidfile ได้
ฆ่า watcher ได้**:
```
PULSE_WATCH_CLAIM_OWNER=<pid ของ watcher ที่รันอยู่> node pulse.mjs watch <repo> --managed
```
ผ่าน → เขียนทับ pidfile → watcher ตัวจริงออกที่ tick ถัดไป · **รันจริงมาแล้ว**
(ensure ตั้ง pid 431211 · หลังรัน 431211 หายไป)

**ทางแก้:** `process.ppid` แทน env var · เคอร์เนลรับรองความเป็นลูก คนอ่าน pidfile
ปลอมไม่ได้ · ชื่อ `PULSE_WATCH_CLAIM_OWNER` หายจากรีโปแล้ว · **ผูกกับ spawn
แม่-ลูกโดยตรง** — ถ้าอนาคตมีใครแทรก `sh -c`/supervisor คั่น มันจะพังตรงนั้น
เสียงดัง ไม่ใช่เงียบ

**ห้ามทำ:** gate `--managed` ด้วยแค่การ*มีอยู่*ของ env var — ปิดทางที่ไม่อันตราย
แล้วเปิดทางฆ่า watcher ทิ้งไว้

### #26 — งบ attempts ไม่รีเซ็ตข้ามรอบ · `f7d34c1`

| ผู้รายงานว่า | ของจริง |
|---|---|
| runner เขียน `escalated` ทุก tick | **ไม่จริง** — มี 5 ตัวกัน ตัวชี้ขาดคือ mark ที่ระบุ controller ทำให้ token เป็น `held` · ledger ของ Master เอง: `escalated` 2 ใบใน 46 บรรทัด |
| ล็อกถาวร | `resumed` รีเซ็ตได้จริง · ราคาคือ PM หนึ่งรอบต่อสามขา — **แพงและผิด แต่ไม่ตาย** |

**ไม่รับข้อเสนอทั้งสองข้อ และวัดแล้วว่าทำไม:**
1. นับเฉพาะขาที่พัง → **ปลดขอบลูปคุณภาพ** `reviewed reject` ไม่ใช่ failure entry
   → จากหยุดที่ 3 รอบ เป็น 7 รอบ ไปหยุดที่เพดาน token 15 · เพดาน role 3 กลายเป็น 15 เงียบ ๆ
2. รีเซ็ตตอน `pulled` → ขัด `loop-system-contract.md:591` และ §9:576-578
   *"Attempts made before the controller looked do not count"* — pull ไม่ใช่ controller looking

**ที่แก้แทน: หลักฐาน** · ประโยคเดียวนั้นคือทั้งหมดที่ controller มีตอนเลือก
`resume` กับ `abandon` · เดิม "3 rejections ที่ทุกขา `done`" กับ "3 ขาตายเพราะ
ประตูล่ม" พิมพ์ประโยคเดียวกัน · ตอนนี้บอกว่าขาจบด้วยอะไร: `ended: done` /
`ended: blocked` / `hard-timeout, lost` · จำนวน legs อยู่ในข้อความเพราะเบรกเทียบ
ไบต์ — ถ้าไม่มีอะไรโต งบที่ใช้หมดรอบสองจะอ่านเหมือนรอบแรกแล้วถูกเบรกกลบ

---

## งานอื่นที่ปล่อยไปแล้ววันนี้

| commit | เรื่อง |
|---|---|
| `3d8e670` | กฎ flow ทางเดียวบังคับที่**บรรทัดที่กำลังเขียน** ไม่ใช่ที่ประวัติ + `sent_back_after_admission` |
| `553f9aa` | ปุ่ม zoom **100%** บนหน้ากราฟ (`fit` = 0.386 · ปุ่มใหม่ = 1.000 พอดี) |
| `cab829e` | เส้น team→dispatcher เป็นสี `--assign` + dashflow ชนะ dry crawl ด้วย weight |
| `f2a6985` | บทเรียน: ห้าม fan out เอเจนต์ที่รันชุดเทสต์เอง |

### `3d8e670` ต้องอ่าน — ผมเกือบทำ token ของ Master ตาย

กฎ one-way ที่ผม ship ตอนเช้า **แช่แข็ง token `story-1-10` ที่ Master ขับอยู่จริง**
· ledger 46 บรรทัด ละเมิดที่บรรทัด 36 · worker ค้างกลางขาที่บรรทัด 46 ·
writer ปฏิเสธการ append ทุกอย่างยกเว้น `abandoned`

**ต้นตอ:** บรรทัด 30 เป็น `returned` ที่ **Master เขียนด้วยมือ** (`actor=human:master`)
เพื่อส่งงานกลับ dev — เพราะ #26 ล็อกงบไว้และระบบไม่เหลือท่าอื่น

**มติ Master:** *"ยืนยันกฏไม่ส่งกลับ รีวิวเวอร์ที่เจอปัญหาก็ต้องแก้ต่อเองให้จบเลย"*

**ผลลัพธ์:** กฎบังคับกับ**บรรทัดใหม่** ประวัติที่ลบไม่ได้ไม่บล็อกก้าวถัดไป ·
pull ย้อนใหม่ยังถูกปฏิเสธ · และเพิ่ม `sent_back_after_admission` ปิดท่าที่ Master
ต้องใช้ตั้งแต่แรก

**ช่องที่ยังเปิด และเขียนลงสัญญาแล้ว:** §1 บอกว่า rework = token ใหม่บน route ใหม่
แต่ **ไม่มีอะไรในระบบสร้าง token ใหม่ได้** — ผู้เขียน `opened` มีรายเดียวคือ
`admit.mjs` ที่คนต้องรันเอง · **Master ไม่ได้แหกกฎ Master ใช้ท่าเดียวที่เหลือ**

---

## วิธีทำงานที่พิสูจน์แล้ว — ใช้ซ้ำ

**ขนานความคิด อนุกรมการวัด**

| | รอบที่ผิด | รอบที่ถูก |
|---|---|---|
| สั่งเอเจนต์ | apply + รันชุดเทสต์ + revert + รันซ้ำ | **อ่านและออกแบบเท่านั้น ห้ามแตะชุดเทสต์** |
| ผล | 42 นาที ไม่มีใครเสร็จ · load 28 · ตัวเลขใช้ไม่ได้ | **18 นาที ครบ 5 ใบ** พร้อม diff ที่ apply ได้ |

สิ่งที่ทำให้ผลใช้ได้จริง:
1. ส่ง**ข้อค้านที่วัดมาแล้ว**ไปกับโจทย์ ไม่ให้เสนอตัวแก้ที่รู้อยู่แล้วว่าพัง
2. บังคับให้ส่ง **diff ที่ apply ได้ด้วย string match** + **คำสั่งที่ผู้เรียกต้องรัน**
   + **mutation หนึ่งบรรทัดที่ต้องทำให้แดง**
3. NOT-READY ทันทีถ้า diff apply ไม่ได้ตรง ๆ
4. **apply ด้วย python ที่ `assert s.count(old)==1` ก่อนเขียนเสมอ**

**mutation test ทุก guard** — วันนี้ 20+ ครั้ง จับได้ 2 ครั้งที่เทสต์เขียวทั้งที่
โค้ดผิด (ปุ่ม 100% ต่อเข้า `reset()` · กฎ send-back ที่ predicate กว้างเกิน)

---

## กับดักที่เสียเวลาไปจริง

- **backtick ในคอมเมนต์ใน template literal** — ครั้งที่ 11 ของโปรเจกต์ เกิดกับ
  `graph.mjs` · ตอนนี้ `plugin-structure.test.mjs` เดินทั้งต้นไม้และ `node --check`
  ทุกไฟล์แล้ว
- **`grep` เป็นเงื่อนไข commit** — คืน 0 ตอนเจอ `✖` · commit ทับแดง 2 ครั้ง
- **รันชุดเต็มถี่เกินจำเป็น** — Master สะดุด 3 รอบ ทั้งที่ fast tier มีอยู่แล้ว
- **สแกนแต่ fixture ในรีโปตัวเอง** — ไม่เคยเปิดรีโปที่ Master ใช้จริง แล้วสรุปว่า
  กฎใหม่ปลอดภัย · advisor เตือนตรง ๆ และผมตอบว่า "ในเครื่องเราไม่มี"
- **เทสต์ที่ปิดสวิตช์ตัวเองแล้วเขียว** — `stallSec: 1e9` · เจอคลาสนี้ 3 ครั้งวันนี้

---

## ค้างอยู่ — เรียงตามที่ควรทำก่อน

1. **รัน `node --test` ให้เขียวบนเครื่องว่าง** แล้วค่อยตัดสินใจเรื่อง version ·
   `acp-companion.test.mjs` แดง 3 ครั้งวันนี้ใต้โหลด **ด้วยชื่อเทสต์คนละตัวทุกครั้ง**
   และผ่านทุกครั้งเมื่อเครื่องว่าง · **`CLAUDE.md` ห้ามเรียกมันว่า flake** —
   ยังไม่มีใครพิสูจน์สาเหตุ ให้ถือว่าเปิดอยู่
2. **bump version + push** ตาม `CLAUDE.md` Release flow (สามที่ + submodule) ·
   ตอนนี้ 7 commit ยังไม่ push
3. **ปิด GitHub #22–#26** พร้อมสิ่งที่แก้จริง**และสิ่งที่รายงานเข้าใจผิด** ·
   #21 เปิดค้างเฉพาะเรื่อง `TMUX_TEAMS_PHASE` ที่ยัง lock vocabulary อยู่
4. **สองข้อที่ Master สั่งแต่ยังไม่ได้ทำ** — เอเจนต์วิเคราะห์เสร็จแล้ว
   (workflow `w2lcc07xd`, transcript ยังอยู่):
   - `● WORKING NOW` กับ `working` แสดงซ้ำบนการ์ดที่กำลังทำงาน · ผลตรวจ:
     `STATUS_COPY` มี 6 ค่า แต่มีค่าเดียว (`working`) ที่โผล่ข้าง flag ได้ ·
     ที่ LEAN zoom ไม่เสียอะไรถ้าเอา flag ออก เพราะ state เป็น `<em>` เหมือนกัน ·
     **`loop-graph-page.md:130-132` อ้างสตริงนี้ตรง ๆ ต้องแก้พร้อมกัน**
   - prefix ชื่อทีม + ชื่อตำแหน่งซ้ำในชื่อโหนด · **`<team>_<role>` เป็นธรรมเนียม
     ไม่ใช่กฎ** — ถ้าใครตั้งชื่อ dispatcher ว่า `alice` การตัดคำต้องไม่ทำพัง ·
     ที่ LEAN zoom บรรทัด `dispatcher · claude · acp` ถูกซ่อน จึงต้องตัดสินว่า
     ตำแหน่งในคอลัมน์กับสีขอบพอเป็นตัวบอก role ไหม
5. **`pull-controller.mjs` ทิ้งค่าที่ writer คืนมา** พิมพ์ลง stderr เท่านั้น —
   pull ที่ถูกปฏิเสธมองไม่เห็นจาก plan · **กินเวลาดีบักไปหนึ่งรอบเต็มวันนี้**
6. tooltip การ์ด kanban (`kanban.mjs:344`) มี `work_item` เต็มกับเวลาสัมบูรณ์
   ที่ไม่อยู่บนการ์ด — คลาสเดียวกับ tooltip แถบบนที่แก้ไปแล้ว
7. หน้ากราฟเลื่อนแนวนอนที่ 400px — `scrollWidth` 538 ทั้งเปิดและปิด panel
   จึงเป็นบอร์ด ไม่ใช่ของใหม่

## ที่อยู่ของของสำคัญ

```
สัญญาระบบ (SSOT)     plugins/tmux-teams/skills/tmux-teams/references/loop-system-contract.md
  §1   flow ทางเดียว + door + rework gap      §3.2.1 seats
  §9   controller + brake identity            §14 AC77–AC81
กฎหน้ากราฟ + วิธีตรวจ references/loop-graph-page.md §9  (เป็นบทของ §12 ไม่ใช่ SSOT ที่สอง)
ผังงาน 6 ภาพ         references/how-it-works.md
review gate          plugins/tmux-teams/skills/party-mode/scripts/review-gate.mjs
hook บังคับปาร์ตี้     .claude/hooks/party-gate.mjs  (gitignored)
ledger จริงที่เคยค้าง  ~/projects/eventbox/.tmux-teams/work-items/story-1-10.jsonl
```

---

# v0.14.3 — บอร์ดว่าง issue เปิด 0 ใบ

สองเซสชันทำงานคู่ขนานกันจากฐานเดียวกัน (`4428b50`) แล้ว merge ที่ `644e45b` ·
**หนี้ข้อแรกของทีมก่อนหน้าปลดแล้ว: `node --test` บนเครื่องว่าง 522 · pass 518 · fail 0**

## หนี้ของทีมก่อนหน้าที่ตอบไปแล้ว

- ~~"`acp-companion` แดง 3 ครั้งใต้โหลด ยังไม่มีใครพิสูจน์สาเหตุ"~~ — **พิสูจน์แล้ว**
  `groupPids()` กับ `waitForGroupGone()` ขึ้นต้นด้วย `process.platform !== 'linux'`
  แล้วตอบว่า "ไม่มีใครอยู่ในกลุ่ม" / "กลุ่มตายแล้ว" **โดยไม่ได้ดู** · บน macOS การกวาด
  descendant ทั้งสองจุดเป็น no-op → **orphan process ค้างทุกครั้งที่ cancel** มาตั้งแต่
  `f528168` (27 ก.ค.) ผ่านสามรีลีส · แก้ที่ `e8c795e`
  · **แต่ยังเหลือความเปราะเรื่องโหลดที่แยกจากบั๊กนี้** — วันนี้เจออีกครั้งตอนรันชุดเทสต์
  ทับกันสองชุด แล้วเขียว 130/130 เมื่อรันตอนเครื่องว่าง · **ยังไม่มีใครพิสูจน์สาเหตุข้อนี้
  ห้ามเรียกว่า flake**
- ~~ข้อ 5 `pull-controller` ทิ้งค่า refusal~~ — แก้ที่ `36571a9` และหนักกว่าที่จด: runner
  **พิมพ์ `pull x: build -> test` เหมือนเขียนสำเร็จ** ซึ่งสัญญา §4.2 ห้ามตรง ๆ

## GitHub — ปิดครบ 8 ใบ

| # | แก้ที่ | หมายเหตุ |
|---|---|---|
| #22–#26 | `5701fb4` `0b1cb72` `3d5b75c` `ea730b0` `f7d34c1` | ของทีมก่อนหน้า · ปิดพร้อมคำอธิบายว่ารายงานระบุกลไกผิดตรงไหน (4 ใน 5 ใบ) |
| #29 | `a2b35ad` | worker ที่รัน tool อยู่ถูกประกาศว่าตายแล้วแทนที่ — **companion ส่ง heartbeat อยู่ตลอด และ pulse ก็ publish แล้ว runner แค่ไม่เคยอ่าน** |
| #23 (รอบสอง) | `8310c22` | ประตู 3 โมเดลพังทุกครั้งในโปรดักชัน — kimi ไม่ล็อกอิน, zai ถูกบังคับ plan mode ที่โมเดลไม่ยอมทำ |
| #30 | `d844d03` | ขาที่ถูกแทนที่เขียน `delivered` ตามหลัง → บอร์ดกับ runner ตามมันกลับทีมเก่า |
| #21 | `245ef6c` | `TMUX_TEAMS_PHASE` ถือคำศัพท์ของระบบที่ลบไปแล้ว · **ไม่มีอะไรในรีโปตั้งมันเลย** จึง retire |

**PR #27** merged · **PR #28** closed พร้อมบันทึกว่าอะไรลง อะไรไม่ลง เพราะอะไร

## สามข้อที่ codex advisor แก้เรา — จำไว้

1. **`git diff a..b` กับ `a...b` ตอบคนละคำถาม** · ผมใช้สองจุดแล้วสรุปว่า "PR ลบบรรทัดใน
   `SKILL.md`" — ที่จริง main เป็นคนเพิ่มบรรทัดนั้นทีหลัง PR ไม่ได้แตะไฟล์นั้นเลย
2. **"ส่วนที่แก้ production ไม่ชนเลย" ผิด** · `main:review-gate.mjs:82-85` เขียนทับทุก
   profile เป็น `plan` — แก้แค่ `review-profiles.mjs` จะไม่มีผลอะไร
3. **สัญญา §6 ไม่ได้ห้ามอ่าน last event ทุกที่** · มันบอกแค่ว่า `teamOccupancy()` เป็นตัว
   เดียวที่ตอบ "ทีมไหนถือ token" · ที่เคลมว่า "11 จุดละเมิด" เกินจริง — ตรวจแล้วมี 8 จุด
   ที่ตอบเรื่องตำแหน่ง/สถานะ อีก 3 จุดอ่านเวลาซึ่งถูกต้องอยู่แล้ว

## กฎที่ได้มาวันนี้

- **เทสต์ยืนยันผลจริงก่อนถ้อยคำที่บรรยายผล** — `assertPidGone` ต้องมาก่อนการเทียบ regex
  ของใบเสร็จ ไม่งั้น orphan ที่ยังวิ่งอยู่โผล่มาเป็น "ข้อความไม่ตรง"
- **กิ่ง platform ที่ตอบไม่ได้ต้องบอก UNKNOWN ห้ามบอกว่า "ไม่มี"**
- **เทสต์ที่ผ่านทั้งที่ถอด guard แย่กว่าไม่มีเทสต์** — วันนี้เจอสองตัว ตัดทิ้งทั้งคู่แล้วเขียนใหม่
- **"ประกาศแล้วไม่ได้พูดอะไร" ต้องปฏิเสธ** — ใช้ตัดสินทั้งเรื่อง seats, `TMUX_TEAMS_PHASE`
  และ stderr ใน report
- **redactor เป็น denylist พิสูจน์ความครบไม่ได้** — วัดแล้ว: ปล่อย OAuth `code=` ใน
  device-flow URL กับชื่อลูกค้าออกมาดิบ ทั้งที่จับ token/apiKey/JWT ได้

## ~~ค้างอยู่~~ — ปิดครบทั้งสี่ข้อ ดูหัวข้อถัดไป

---

# รอบสอง (3 ส.ค.) — ปิดรายการค้างทั้งสี่ข้อ

ยังไม่ bump version · commit `668a14f a7066cf 92c0c35 096cab0` · `node --test` **527/523/0**

## 1. B4 — reason code ปิดชุด · `a7066cf`

`runnerEvidenceError` คืน **ประโยคอิสระ 12 แบบ** ที่บอกว่า gate *สังเกตเห็นอะไร* ไม่เคยบอก
*ต้องทำอะไร* · `LANE_FAILURES` เป็นตารางปิด 13 รหัส แต่ละรหัสมี `text` กับ `action`
· `attempts[].action` derive จากตารางเดียวกันจึงขัดกันไม่ได้ · หลายรหัสบอก **DO NOT RETRY**
ซึ่งเป็นข้อเท็จจริงที่ประโยคเดิมไม่เคยพก — lane ที่ติดป้ายผิดกับ provider ที่ช้าเคยอ่าน
เหมือนกันหมดว่า "พังแล้ว ลองใหม่"

**action ไปโผล่ที่ stderr ด้วย** — stdout คือสำเนาของเครื่องและถูก redirect ลงไฟล์
action ที่ไปจบใน JSON ที่ไม่มีใครเปิดไม่ใช่ action

**บทเรียน:** เทสต์ stderr ใบแรกของผม **ผ่านทั้งที่ mutate guard** — fixture มีแต่ lane ที่
accepted ซึ่งไม่มี `reason` อยู่แล้ว guard สองแบบจึงให้ผลเท่ากัน · เขียนใหม่ให้มี lane ที่
failed แต่ไม่มี action

## 2. ความเปราะของ `acp-companion` ใต้โหลด — **พิสูจน์สาเหตุแล้ว** · `668a14f`

**ไม่ใช่ flake และไม่ใช่ timeout ของ helper** · จำลองได้จริงที่ 2.2 เท่าของเวลาเครื่องว่าง
(สองชุดซ้อน + spinner 10 ตัว · 98.5 วิ เทียบ 44.8 วิ) แล้วได้

```
tests/acp-companion.test.mjs:2251   actual: 'stalled'   expected: 'cancelled'
```

สามใบตั้ง `MOCK_EXIT_DELAY_MS: 5` คู่กับ `ACP_CANCEL_GRACE_MS: 80` แล้วประกาศใน**ชื่อของ
ตัวเอง**ว่า *"the child exited BEFORE the grace"* · 80ms ต่ำกว่าพื้นเสียงของ scheduler บน
เครื่องที่ไม่ว่าง → **ข้อสมมติกลับหัว** เทสต์ไปวัดสาขา forced แทน แล้วรายงานออกมาเป็นค่าที่
ไม่ตรง ซึ่งอ่านเหมือน regression เป๊ะ

**grace คือเพดาน ไม่ใช่การหน่วง** — child ออกที่ 5ms และ companion settle ทันที เพดาน 5 วิ
จึงไม่มีวันถูกแตะบนเครื่องว่าง · **ยืนยันว่า assertion ยังบิดไม่ได้:** ตั้งเพดานเป็น 1ms
แล้วแดงตรงสามใบนั้นพอดี — ยังแยกสองสาขาอยู่ ไม่ได้ถูกสมมติเอา

สองใบที่ตั้ง 20ms **ไม่แตะ** — mock ของมันไม่ออกเอง grace หมดเสมอ กลับหัวไม่ได้

**วัดหลังแก้:** สามชุดซ้อนที่ 95.5 / 98.2 / 98.2 วิ = แรงกดเท่ารอบที่แดง · 130/130 ทั้งสาม

## 3. #30 ครึ่งที่เหลือ — leg identity · `92c0c35`

**leg identity ถูกบันทึกมาตลอด ไม่มีใครอ่าน** · `ledger-validate.mjs:83` บังคับ `dispatch_id`
บน `assigned` และ `appendWorkItemEvent` ประทับลงทุกบรรทัดที่ companion เขียน `delivered`
จึงมีอยู่แล้วฟรี ๆ

`currentEntry` เทียบมัน **เฉพาะเมื่อทั้งสองฝั่งมี** · `reviewed` เข้าชุดได้แต่**ไม่มี fallback
เป็น agent_id** ส่วน `delivered`/`lost` เก็บ fallback ไว้ · กิ่งที่ตอบไม่ได้คืน entry เหมือนเดิม
— **ledger เก่าอ่านได้เท่าเมื่อวานทุกประการ**

**ปิดข้อจำกัด same-agent ของ `delivered` ให้ฟรี** เพราะ agent_id อ่านเหมือนกันทั้งสองขา
แต่ dispatch_id ไม่

**mutation 4 แบบแดงหมด** · ใบที่สำคัญที่สุดคือ D (ให้ `reviewed` มี fallback agent_id) →
แดงสี่ใบ ซึ่งคือสี่ใบเดิม = พิสูจน์ว่ากฎ "ห้าม fallback" คือตัวที่กันมันไว้

**ฝ่ายค้านจับได้หนึ่งข้อและถูกต้อง:** เทสต์ใบสองตามที่เสนอมา assert แค่ `teamOccupancy`
ซึ่งเป็นระดับ**ทีม** — สองขาเป็น agent เดียวกันจึงอยู่ทีมเดียวกัน อ่านเท่ากันไม่ว่าจะเลือกขาไหน
**ผ่านทั้งที่ยังไม่แก้บั๊ก** · เขียนใหม่ให้ assert ที่ `currentEntry` ตรง ๆ

## 4. tooltip การ์ด kanban · `096cab0`

Master ตัดสิน: **ยกขึ้นมาบนการ์ด** · id เต็ม (ledger อนุญาต 128 ตัว การ์ดตัดที่ 60 = ตัดสิ่งที่
ระบุตัวการ์ดเอง) + `<time>` เวลาสัมบูรณ์ · **ถอด `title` ทิ้ง** ไม่ใช่ตัดให้สั้น — tooltip ที่ซ้ำกับ
ข้อความข้าง ๆ ไม่ใช่สำเนาสำรอง มัน**ทับ accessible name** screen reader จะอ่านบทสรุปแทนการ์ด

**ไม่ใช่แบบเดียวกับ `197a8e1`** ที่ย้ายข้อมูลลง tooltip — อันนั้นถูกสำหรับข้อมูลที่คนอ่านไม่ต้องใช้
อันนี้คือข้อมูลที่คนอ่านต้องใช้แล้วเอื้อมไม่ถึง

## ค้างอยู่

1. **hole (b) ของ `lost`** — `lost` เขียนโดย runner (`loop-runner.mjs:1230`) และไม่พก
   `dispatch_id` · ค่ามันอยู่ห่างไปหนึ่ง hop ที่ `last.dispatch_id` ใน `nextStep` แต่ object
   ที่ return ไม่ได้ใส่ · สองขาของ agent เดียวกันที่ขาเก่าจบด้วย `lost` ยังแยกไม่ออก
2. **ยังไม่ bump version** — ของทั้งหมดยังไม่ถึงผู้ใช้จนกว่าจะ bump สามที่ + marketplace update
3. หน้ากราฟเลื่อนแนวนอนที่ 400px (มีมาก่อนแล้ว)

## กฎที่ได้มารอบนี้

- **grace คือเพดาน ไม่ใช่การหน่วง** — ยกเพดานที่ไม่เกี่ยวกับเรื่องที่เทสต์พิสูจน์ ไม่ใช่
  "ยก timeout หนีปัญหา" ถ้า assertion วัดข้อเท็จจริงที่ถูกบันทึก ไม่ได้วัดความเร็ว
  · แต่ต้องพิสูจน์ด้วย mutation ว่ามันยังบิดไม่ได้ ไม่งั้นมันคือการหนีจริง ๆ
- **เทสต์ที่ประกาศข้อสมมติไว้ในชื่อตัวเอง ต้องบังคับข้อสมมตินั้น** — ไม่ใช่หวังว่าเครื่องจะเร็วพอ
- **assertion ระดับรวมกลุ่มพิสูจน์เรื่องระดับหน่วยไม่ได้** — `teamOccupancy` ระดับทีม ตอบเรื่อง
  "เลือก leg ไหน" ไม่ได้ · ใบนั้นดูเหมือนมี coverage แต่ไม่มี
- **mutation ที่ revert ไม่ได้คือ mutation ที่ออกแบบผิด** — anchor `verdict,` โผล่ 7 ที่
  ทำให้ไฟล์ค้างสภาพ mutate แล้วผลรอบถัดไปเป็นโมฆะ · anchor ต้อง unique ทั้งขาไปและขากลับ

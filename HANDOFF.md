# HANDOFF — tmux-teams · **v0.18.2 ปล่อยแล้วครบทุกช่องทาง** (2026-08-09)

> เขียนให้ **agent** อ่านแล้วลงมือต่อได้ทันที
> อ่านสามหัวข้อแรกแล้วรู้ว่าต้องทำอะไรต่อ

## อ่านก่อนอื่น

- **อยู่บน `main` · ไม่มีอะไรค้าง ไม่มีอะไรบล็อก** — v0.18.0 → 0.18.1 → 0.18.2
  ปล่อยครบทุกช่องทางทั้งสามตัว (push · CI เขียว · tag · release page · marketplace)
- ✅ **อิชชูเหลือ 0 ใบที่ต้องแก้** — ใบ bwrap sandbox **เปิดค้างโดยตั้งใจ**:
  โค้ดแก้แล้วและ unit test แล้ว แต่บล็อกนั้นรันได้เฉพาะ Linux ที่มี bwrap
  **เครื่องนี้ไม่มีทั้งคู่ จึงยืนยัน e2e ไม่ได้** · คอมเมนต์ในใบเขียนคำสั่งที่ต้องรันไว้แล้ว
- ⚠️ **v0.18.0 ปล่อยด้วยคำสั่ง Master ไม่ใช่ด้วยเกต** — เกตสามใบ **ไม่เคยครบสักรอบใน 8 รอบ**
  · รอบ 8 บล็อก 2/3 · แก้ครบแล้วแต่ **ไม่ได้รีวิวซ้ำก่อนสแตมป์** · จดไว้ให้ตรงตามจริง
  · 0.18.1 กับ 0.18.2 ก็ไม่ได้ผ่านเกตเช่นกัน
- ✅ **เกตถูก "ปรับขอบเขต" แล้ว ไม่ได้ยกเลิก** (Master 9 ส.ค.) — `node scripts/gate-required.mjs`
  ตอบว่ารีลีสนี้ติดเกตไหม **จาก `git diff` ไม่ใช่จากวิจารณญาณคนปล่อย** · `0`=ยกเว้น `2`=ต้องเข้าเกต
  · **ยกเว้นได้เฉพาะเอกสารกับเลขเวอร์ชัน และต้องบันทึกใน release notes เสมอ**
  · รันย้อนหลังสามรีลีสล่าสุด → **ตอบ REQUIRED ทั้งสาม** กฎใหม่ไม่ได้ล้างบาปเก่า
- 🔵 **submodule pin ใน `~/agent-skills` ยังไม่อัป** — คนละโปรเจกต์ ต้องขอ Master ก่อน

> **ห้ามเขียนตัวเลขที่เปลี่ยนได้ลงในไฟล์นี้ — เขียนคำสั่งแทน** เพิ่ม 8 ส.ค. หลัง
> **ทั้งสามเลนบล็อก/ทักเรื่องเดียวกัน**: ฉบับก่อนมีจำนวนคอมมิตขัดกันเองสามชุด (11/31/28)
> ทั้งที่ตอนเขียนแต่ละบรรทัดถูกหมด — **มันเก่าเพราะคอมมิตถัดไปลง ไม่ใช่เพราะใครสะเพร่า**
>
> ```bash
> git describe --tags --abbrev=0        # รีลีสล่าสุด
> git status -sb | head -1              # ahead เท่าไร
> node --test 2>&1 | grep -E '^ℹ (tests|pass|fail)'
> gh issue list --state open
> ```

## 🗺️ Roadmap — เผยแพร่แล้ว อ่านอันนี้ก่อนวางแผนใหม่

**https://artifacts.ngs.bz/claude/private/tmux-teams-next-plan/** (v6 เขียนแล้วในเครื่อง —
**ยังไม่ได้เผยแพร่** · สกิล `artifact-sftp` ไม่ได้ติดตั้งใน plugin cache ของเครื่องนี้)

ห้าเฟส เรียงตามหลักการของโปรเจกต์เอง · สำเนาอยู่ที่
`docs/artifacts/claude/private/tmux-teams-next-plan/index.html` (ไม่ถูก commit — ตั้งใจ)

| เฟส | คือ | สถานะ |
|---|---|---|
| **A** | ปิดคำอ้าง 2 ข้อที่ไม่มี guard — `mcpServers: []` · เชื่อโมเดลที่ขอ↔ที่ตอบ | **แนะนำทำต่อ** ~ครึ่งวัน |
| **B** | เกตรีวิวสามโมเดล — **ปรับขอบเขตแล้ว** (Master 9 ส.ค.) | ✅ **เสร็จ** |
| **C** | ยืนยัน sandbox fix บน Linux | รอเครื่อง |
| **D** | กฎ §14.1 ที่เหลืออีก 5 ข้อ | ยังไม่ถึงเวลา |
| **E** | ให้ทีมประกาศกระบวนการเอง (`nextStep` 305 บรรทัด 10 ทางออก) | ไม่ทำจนกว่าจะเจ็บ |

**§14.1 ของสัญญาคือรายการ "ยังไม่สมบูรณ์" ที่แท้จริง** — 7 กฎที่โค้ดทำตามอยู่โดยไม่มีอะไรกัน
ไม่ให้ถอยกลับ · สองข้อบนสุดเป็น**ขอบเขตความปลอดภัยที่ค้ำด้วยการอ่านโค้ดเท่านั้น**

## v0.18.2 — สกิล advisor รับพารามิเตอร์แล้ว

`claude-advisor <bin> <model>` · `codex-advisor [luna|terra|sol]` (effort ล็อก `max`)

**ตารางเส้นทางอ่านจาก `settings.json` ของแต่ละโปรไฟล์จริง** ไม่ได้เขียนจากความจำ:
`claude-kimi opus`→`k3` · `claude-qwen opus`→`qwen3.8-max` ·
`claude-qwen sonnet`→`deepseek-v4-flash-0731` · `claude-zai opus`→`glm-5.2[1m]`

สองเรื่องที่การวัดบังคับให้เขียนลงสกิล:
- **alias ไม่ใช่ตระกูล** — `opus` บนสาม bin ไปถึงสามผู้ให้บริการ · receipt ที่เขียน
  `effective_identity: opus` บอกแค่ช่องที่ขอ **ไม่ได้บอกว่าใครตอบ** · พิสูจน์ตระกูลจาก endpoint
- **เส้นทางมีสองวิธีสะกด เครื่องนี้ใช้ได้วิธีเดียว** — `~/.local/bin` มีแค่ `claude`
  **ไม่มี `claude-*` สักตัว** แต่โปรไฟล์ครบ → `CLAUDE_CONFIG_DIR` คือแบบที่ยืนยันแล้ว
  ส่วน `CLAUDE_CODE_EXECUTABLE` ยังไม่เคยรันสำเร็จที่นี่

**เลนรีวิว codex ยังปักที่ `ultra` ขณะที่สกิล advisor ล็อก `max` — ตั้งใจให้ต่างกัน**
เขียนกำกับไว้ในสกิลแล้ว ไม่งั้นจะมีคนมาปรับให้ตรงกันแล้วเรียกว่าจัดระเบียบ

- **CI เขียว** ตั้งแต่ `3c5cfde` — ก่อนหน้านั้น**แดงมาสองรีลีสโดยไม่มีใครดู**
- **ยังไม่ commit:** `docs/` (ไม่อยู่ในรายการ tracked ของ repo นี้ — ตั้งใจ)

## 🚧 เกต v0.18.0 — ห้ารอบแล้ว ยังไม่ผ่าน อ่านตรงนี้ก่อน

| รอบ | AGY (gemini) | zai (glm-5.2) | codex (gpt-5.6) | ผล |
|---|---|---|---|---|
| 1 | **BLOCKED** | TEAM_DONE | ตายกลางทาง (โควตา) | แก้ |
| 2 | APPROVED | TEAM_DONE | **BLOCKED** 3 ข้อ | แก้ |
| 3 | APPROVED | APPROVED | **BLOCKED** 6 ข้อ | แก้ |
| 4 | APPROVED | APPROVED | **BLOCKED** 1+3 ข้อ | แก้ |
| 5 | APPROVED | APPROVED | **BLOCKED** 1+2 ข้อ | แก้ + ล่าคลาสถัดไปเอง |
| 6 | **BLOCKED** | **BLOCKED** | **BLOCKED** 4+3 ข้อ | แก้ครบ (`fba7da7` + `d18fbbb`) |
| 7 | APPROVED | APPROVED | **BLOCKED** 4 ข้อ | แก้ครบ (`c7d04ba`) |
| 8 | **BLOCKED** 2 | APPROVED (4 ข้อไม่บล็อก) | **BLOCKED** 6 ข้อ | แก้ครบ (`b11741f`) → **ปล่อย** |

**เกตไม่เคยผ่านครบสามใบเลยแม้แต่รอบเดียว** — ปล่อยด้วยคำสั่ง Master หลังแก้ครบทุกข้อ
**ถ้าจะรื้อเรื่องนี้ อ่านตรงนี้ก่อน: 8 รอบ · finding ที่เป็นบั๊กโค้ดจริง 8 ตัว · ที่เหลือเป็นประโยค**

**รอบ 8 คือรอบที่หักล้างข้อสันนิษฐานของห้องนี้เอง** — ก่อนรันเราพูดว่า "สามรอบหลังไม่มี
finding ที่เป็นบั๊กโค้ดเลย" **รอบ 8 มีสองตัว และวัดยืนยันแล้วทั้งคู่** ดูหัวข้อ 🔴

**รอบ 6 คือจุดเปลี่ยน — บล็อกทั้งสามเลนพร้อมกัน** หลังจาก AGY กับ zai ผ่านติดกันสามรอบ
และตัวที่ **AGY กับ zai เจอตรงกันโดยอิสระ** คือ **regression ที่การแก้ของเราเองสร้าง**

**codex บล็อกสามรอบติด และทุกใบจริง — ยืนยันด้วยการวัดทุกข้อ ไม่มีข้อไหนเถียงทิ้ง**
จำนวนลดลงทุกรอบ (6 → 4 → 3) แต่**ทุกรอบเป็นคลาสใหม่ที่รอบก่อนนึกไม่ถึง**

## ✅ รอบ 8 — หกข้อ แก้ครบที่ `b11741f` (เก็บไว้เพราะรูปแบบมันซ้ำ)

**1. receipt ที่เป็น symlink = การถอนหายถาวร** (`loop-runner.mjs:1944-1950`)

`markNoticeRead()` ใช้ `writeFileSync` ซึ่ง**เดินตาม symlink** → เขียนสำเร็จ คืน `true`
`noticeWasRead()` ใช้ `lstatSync` + `isFile()` ซึ่ง**ปฏิเสธ symlink** → อ่านไม่ผ่านตลอดกาล

ผลลัพธ์: `unmarked` ว่าง → `pm-notes/latest.md` บันทึกว่าถอนแล้ว → tick ถัดไปสร้าง trigger
เดิมเป๊ะ → เบรก unchanged-trigger กดทิ้ง **ถาวร** — **คือเส้นทางที่ AC124 อ้างว่าแก้แล้ว**

> แก้: ให้ `markNoticeRead` ปฏิเสธ symlink ด้วยเงื่อนไขเดียวกับตัวอ่าน
> (`{ flag: 'wx' }` หรือ `lstat` ก่อนเขียน) · **ผู้เขียนกับผู้อ่านต้องใช้นิยาม "ไฟล์" เดียวกัน**

**2. OSC ทะลุ marker quoting** (`acp-companion.mjs` · `ANSI_OR_CONTROL`)

วัดจากไบต์ที่ ship (สไลซ์ source ออกมา eval ไม่ได้พิมพ์ regex ใหม่ —
`<scratchpad>/osc-probe.mjs`):

```
CSI erase   visible="TEAM_DONE t1"                 forgeable=true   ← กันได้
OSC + BEL   visible="0;window titleTEAM_DONE t1"   forgeable=false  ← หลุด
OSC + ST    visible="0;window titleTEAM_DONE t1"   forgeable=false  ← หลุด
```

`ESC ]` ถูกลบ แต่ payload `0;window title` เหลือค้าง → anchor ไม่แมตช์ → บรรทัดออกดิบ ๆ
ขณะที่ **terminal กลืน OSC ไปตั้งชื่อหน้าต่าง แล้วโชว์ `TEAM_DONE t1` สะอาด**
· **AC127 อ้างว่าครอบคลุม control sequence — ครอบคลุมแค่ CSI**

> แก้: OSC = `ESC ]` … จบด้วย `BEL` หรือ `ESC \` — ต้องลบ**ทั้งลำดับ** ไม่ใช่แค่ตัวเปิด
> · และ **DCS/SOS/PM/APC ก็ปิดด้วย ST เหมือนกัน — ไปนับให้ครบก่อนเขียนว่า "ครอบคลุม"**

**3. เทสไม่ hermetic — `ACP_MODEL` รั่วจาก `process.env`** (zai วัดสองทาง)

`chat-marker-quoting.test.mjs` + `worker-isolation.test.mjs` สร้าง env ด้วย `{...process.env}`
· `ACP_MODEL=opus node --test` → **pass 789 fail 12** · `env -u ACP_MODEL` → **801/0**
**เครื่องที่รันรีวิว ACP มีตัวแปรนี้เสมอ** — คลาสเดียวกับที่ `0e6ddf7` เพิ่งแก้ไป

**4. guard `[terminal]` ไม่นับ backtick** — ทั้งสามเลนพูดตรงกัน (zai บอกตั้งแต่รอบ 7
ในฐานะ non-blocking **แล้วผมข้าม** รอบ 8 กลับมาเป็น blocking สองเลน)
· ``line.includes(`[terminal]`)`` เดินผ่าน · ต้องระวังไม่ให้ชนตัวเขียนเอง ซึ่งเป็น
`` `[terminal] ${...}` `` — จับ backtick ที่ตามด้วย token แล้วปิดโดยไม่มี `${`

**5. `plugin.json` guard ยังไม่ตรวจ schema จริง** — รับ `author: "IIC Master"` ได้
ทั้งที่ schema บังคับว่าเป็น object · และคอมเมนต์ในเทสเขียน "FIFTH place" ซ้ำความผิดเดิม

**6. `review-gate.mjs:612-621` สรุปสาเหตุร่วมผิด** — `config` ล้มคนละเหตุสามแบบก็ได้
`stages.size === 1` เหมือนกัน แล้วพิมพ์ว่าเป็นสาเหตุเดียวกัน

## ✅ ตัดสินแล้ว — ทาง C (Master, 8 ส.ค.) · ทำครบที่ `d18fbbb`

**คำตัดสิน: log ไม่ใช่พยาน — ลบประโยคที่แต่งตั้งมัน ไม่ใช่ไล่ปิดเครื่องแต่งกายรอบที่เจ็ด**

สิ่งที่วัดได้ก่อนตัดสิน (ทั้งเรพ + ทั้งเครื่อง):

| ถาม | วัดได้ |
|---|---|
| stdout ของ companion ไปไหน | `loop-runner.mjs:1872` `stdio: ['ignore', logFd, logFd]` → ไฟล์ใน `.tmux-teams/runner-logs/` |
| runner อ่านคำตัดสินจากไหน | `loop-runner.mjs:692` — ไบต์ของ**ไฟล์ outbox** ผูกด้วย `outbox_digest` ที่จดตอน `delivered` |
| ใครอ่านบรรทัด `[terminal]` | **ไม่มี** — ทั้งในเรพและนอกเรพ (hit นอกเรพคือสำเนาปลั๊กอินเอง) |
| เรพเคยเขียนไว้เองไหม | เขียนแล้วสองที่ — `kms.mjs:29` และ `teammates-messaging.md:145` (*"not the data path"*) |

**แปลว่าสิ่งเดียวที่ทำให้ stream คุ้มค่าแก่การปลอมคือคอมเมนต์ของเราเองที่ตั้งมันเป็นพยาน**
ประโยคนั้นผิดตั้งแต่วันที่เขียน และขัดกับไฟล์อื่นในต้นไม้เดียวกัน

ทำไปแล้วใน `d18fbbb`:
1. **ลบคำอ้าง** ทั้งสองจุด แทนด้วยความจริง — log เป็น diagnostic · quoting ปกป้อง**คนอ่าน** ·
   ทางเดินเครื่องคือไฟล์ที่ผูกแฮช
2. **เทสค้ำครึ่งหลัง** — `plugin-structure.test.mjs` เดินต้นไม้ที่ ship แล้วปฏิเสธสองรูปแบบที่
   "ตัวอ่าน" ต้องใช้ (regex กับ quoted string) · **mutation แดงทั้งสองแบบ restore ตรงแฮช**
3. **`user_message_chunk` quote แล้ว** — เดิมถูกยกเว้นโดยไม่มีเหตุผลจดไว้ · **authorship ตัดสิน
   ไม่ใช่ provenance** · mutation ยืนยัน
4. สัญญา **AC130, AC131**

**สิ่งที่ทาง C ไม่ได้แก้ และต้องพูดเองก่อนโดนถาม:** คนที่นั่ง tail log ยังโดนหลอกด้วยตาได้
C ตัดเหยื่อที่เป็น**เครื่อง** ไม่ได้ตัดเหยื่อที่เป็น**คน** — เขียนไว้ในคอมเมนต์แล้วว่านั่นคือขอบเขต

### ประวัติของคำถามนี้ — เก็บไว้เพราะรูปแบบมันซ้ำ

สองข้อที่ตั้งใจไม่แก้ตอนรอบ 6 (codex วัดแล้วทั้งคู่):
- **บรรทัด `[terminal]` ปลอมได้จาก chat** — agent พิมพ์ `[terminal] ...` ออกมาดิบ ๆ
  ทั้งที่คอมเมนต์ในโค้ดเขียนว่า "บรรทัดเดียวที่บอกคำตัดสิน"
- **`user_message_chunk` ไม่ถูก quote เลย** — history ที่ replay มามี marker ได้

**นี่ไม่ใช่บั๊กของ regex — คือ stream ไม่มีการแยกโครงสร้างระหว่างบรรทัดของ companion
กับบรรทัดของ agent** หกรอบพิสูจน์แล้วว่าไล่ปิดเครื่องแต่งกายทีละแบบเป็น arms race:
ทุกครั้งที่ปิดคลาสหนึ่ง คลาสถัดไปเปิด (chunk → ANSI → zero-width → CGJ → CRLF)

ตอนนั้นเสนอสี่ทาง — **A** prefix ทุกบรรทัด (ปิดถาวร แต่ log format เปลี่ยน) · **B** patch
แล้วเข้ารอบ 7 · **C** ลบคำอ้าง · **D** แยกรีลีส ตัดงาน quoting ไป 0.18.1

**Master เลือก C** และตอบว่า "ไม่แน่ใจ" เรื่องตัวอ่านนอกเรพ — คำตอบนั้นคือเหตุผลที่ต้องไปกวาดหา
ก่อนแก้โค้ด และผลคือ**ไม่มี** ถ้าคำตอบเป็น "มี" ทาง C จะไม่พอและ A คือคำตอบ

> **บทเรียนที่ใหญ่กว่าตัวบั๊ก: หกรอบไล่ปิดคลาสทีละแบบ โดยไม่มีใครถามว่าเหยื่อคือใคร**
> คำถาม "ใครอ่านสิ่งนี้" ใช้เวลาวัดสิบนาที และมันลบคำถามทั้งหมดที่เหลือทิ้ง

### สิ่งที่ codex เจอ เรียงตามคลาส (ทั้งหมดวัดแล้วว่าจริง)

| รอบ | คลาส | สาระ |
|---|---|---|
| 3 | receipt อยู่ในข้อความที่ worker เขียนได้ | ย้ายเป็น sidecar แล้ว **แต่คำอ้างว่า worker แตะไม่ได้ยังผิด** — worker รันในเรพนั้น |
| 3 | log สัญญาสิ่งที่โค้ดทำไม่ได้ | mark ล้ม → เบรก unchanged-trigger กดทิ้ง**ถาวร** |
| 3 | สัญญาพูดกว้างกว่าโค้ด | "ปิดทุกชั้น" ตอนที่ root กับ role map ยังเปิด |
| 4 | **เทสไม่เคยแตะสิ่งที่มันตั้งชื่อ** | `clearStaleOutbox` ปฏิเสธก่อน — `readTerminalOutbox` ไม่เคยรัน · ข้อความเหมือนกันเลยเขียว |
| 5 | **เครื่องแต่งกาย** | `ESC[2K` หน้า marker → anchor ไม่แมตช์ แต่ terminal ลบบรรทัด = marker ปลอมสมบูรณ์ |

### คลาสที่ 6 — เราล่าเอง ไม่ได้รอให้บอก

หลังแก้ ANSI เสร็จ ถามว่า**การแก้นี้เพิ่งเปิดคลาสอะไร** แล้ววัด:
zero-width space · ZWJ · BOM · bidi override · word joiner · soft hyphen · NBSP indent
· trailing ZWSP — **9 probe รั่ว 9** ปิดหมดแล้ว (AC128)

**นี่คือรูปแบบที่ควรทำทุกครั้งหลังแก้เรื่อง parsing/anchor** ราคาสิบนาที เทียบกับรีวิวหนึ่งรอบ

### หลักฐานตระกูล — บันทึกครบทั้งสามเลน (จำเป็นบนทาง ACP ตรง)

| เลน | adapter | effective_identity | หลักฐานว่าเป็นคนละตระกูล |
|---|---|---|---|
| agy | `antigravity-acp@1.0.0` | `gemini-3.6-flash-high Gemini 3.6 Flash (High)` | `identity_status: unverified` — **ข้อยกเว้นที่อนุญาตเฉพาะเลนนี้** |
| zai | `claude-agent-acp` 0.66.0 | `opus` | **`opus` ไม่พิสูจน์ตระกูล** — โปรไฟล์ map `opus` → `glm-5.2[1m]` ที่ `api.z.ai` |
| codex | `codex-acp@1.1.7` | `gpt-5.6-terra[max]` | หมุด 1.1.7 อย่าเลื่อน |

**รอบ 7 (8 ส.ค.)** — `<scratchpad>/r18g/{agy,zai,codex}/` · จดครบทั้งสามเลนก่อนทำอะไรต่อ:
`agy 44b16fbf-68b0-4e90-9efd-7ac1b260280e` (unverified) ·
`zai 4603d50c-8731-4839-9576-fb1faace912f` (**`matched` รอบนี้** ต่างจากรอบ 6) ·
`codex 019fe0a9-7cf9-7381-951a-01febe414736` (unverified)

**บทเรียน:** บนเกตเวย์ร่วม ชื่อ alias ที่ receipt บันทึกไม่ได้บอกว่าใครอ่าน ต้องอ่าน
`ANTHROPIC_DEFAULT_OPUS_MODEL` + `ANTHROPIC_BASE_URL` ของโปรไฟล์ประกอบ

**session id ทุกเลน ทุกรอบ อยู่ใน `<scratchpad>/r18{c,d,e}/*.log`** — `grep '^\[session\]'`
ห้าม `rm -rf` ไดเรกทอรีรันก่อนจดไว้

### กฎที่ห้ามละเมิดตอนปล่อย

> **รีวิวที่รับได้น้อยกว่าสามใบ = รีลีสถูกบล็อกอย่างเห็นได้ · ห้ามสับเปลี่ยนเงียบ ห้ามลดเหลือสองคน**

**สองใบผ่านไม่เคยแปลว่าพอ** — AGY กับ zai ผ่านมาสามรอบติดกันในขณะที่ codex เจอของจริงทุกรอบ

**กับดัก codex:** `execution_profile_drift` = adapter หายจากแคช (จาก `npm cache clean --force`)
แก้: `npx -y @agentclientprotocol/codex-acp@1.1.7 --version`

## v0.18.0 — นับคอมมิตด้วย `git rev-list --count v0.17.0..HEAD` (อย่าเขียนเลขไว้ตรงนี้)

**เลข 0.18.0 ไม่ใช่ 0.17.1 เพราะมี breaking สองข้อ:**
- กราฟที่ประกาศคีย์เกิน **เคยโหลดผ่าน ตอนนี้ถูกปฏิเสธ**
- worker เลน claude **เปลี่ยนค่าปริยาย** เป็นแยกตัวจากโปรเจกต์

**ธีม: ความผิดที่ไม่ส่งเสียง** — 9 อิชชูปิดหมด ทั้งหมดเป็นความล้มเหลวที่ดูเหมือนปกติ

| เรื่อง | สิ่งที่พบจริง |
|---|---|
| worker พิมพ์ `TEAM_DONE` ในแชท | **companion ถูกอยู่แล้ว — log ของมันหลอกคนอื่น** |
| ที่นั่งว่างแดง 90 นาที | **ฟังก์ชันสร้างความพังที่คอมเมนต์ตัวเองบอกว่ามีไว้กัน** |
| review-gate กลืน error | ข้อความถูกทิ้ง **3 ชั้น** — เจอเองตอนรันเกตรีลีส |
| marker วางผิดที่ | บอกกฎ ไม่ใช่แค่บอกอาการ · แยก 3 กรณีที่ worker ต้องแก้คนละแบบ |
| `pm.md` ทับ brief | ต่อท้ายแทนทับ · **ทุก role ไม่ใช่แค่ pm** |
| โทเค็นถอนค้าง | `abandoned` เป็น hard terminal — **receipt ไปอยู่ที่ notice** |
| worker สืบทอด hook | `CLAUDE_CODE_SIMPLE=1` (= `claude --bare`) · **วัดด้วย hook เขียนไฟล์จริง** |
| เส้นวิ่งซ้อน | pulse ไม่มี `work_item` — **ไม่ขยายสัญญาเพื่อความสวย** เขียนใน legend แทน |
| context บวม | **ไฟล์ที่ขอให้แก้ไม่ได้อยู่ในเรพนี้** (`resolve_party.py` เป็นของ BMAD) |

## 🔴 CI — กฎใหม่ อ่านก่อนปล่อย

**CI แดงมาตั้งแต่ก่อน v0.16.0 และ v0.17.0 ออกไปบนมัน**

เกตใน CLAUDE.md เดิมมีสามอย่าง **ทั้งหมดเป็นเกตในเครื่อง** · เครื่องอ่าน 793/0 · **CI อ่าน 785/2**

**เพิ่มเป็นข้อ 6 ของ Release flow แล้ว** — หลัง push เพราะ push คือตัวจุดชนวน

**สามรอบกว่าจะเขียว และโทษผิดสองรอบ:**

1. เทสอ่าน `~/.config/claude-profiles/kimi/` ของคนเขียน — **ถูก**
2. "CI ส่งอีเวนต์ fs.watch ไม่ได้" — **ผิด** probe สร้างไฟล์ใหม่ ไม่ได้ append
3. "CI append ไม่ได้" — **ผิด** · **ลูป retry เขียนทุก 25ms ทับ debounce 30ms** ทุกการเขียนรีเซ็ต timer

**บทเรียน: CI พูดถูกสามครั้ง** · ค่าของเครื่องที่สองคือมันไม่แชร์สมมติฐานกับเรา

**ข้อเท็จจริงของแพลตฟอร์ม (บันทึกในสัญญาแล้ว):** บนบางไฟล์ระบบ การสร้างไฟล์ใหม่ถูกรายงาน
แต่ **append ไฟล์เดิมไม่ถูกรายงาน** — ซึ่งคือรูปแบบที่ `ledger-writer.mjs` ใช้ ·
คำตอบของคนจะถูกเห็นโดย interval แทน watcher · **นั่นคือความต่างระหว่างช้ากับผิด**

## อิชชู — เปิดค้าง 1 ใบ โดยตั้งใจ · เช็คด้วย `gh issue list --state open`

- ✅ **`#61` carve-out ที่ไปไม่ถึง — ปิดแล้ว 8 ส.ค.** · `workflow-graph.mjs:208` ดัก
  `downstream_team_id` แล้ว `return` ทันที · carve-out ที่บรรทัด 274 จึงเป็นโค้ดตาย
  · **โค้ดที่เพิ่งเขียนวันนี้** · แก้ = ลบ carve-out แล้วเขียนคอมเมนต์ว่าใครเป็นเจ้าของคีย์นั้น
- **`#60` ไม่มีเลน claude-routed ไหนรันใน bwrap ได้** — `CLAUDE_CODE_EXECUTABLE` ไม่เคยถูก stage
  → ทุกเลนตายที่ `session/new` เหลือ AGY ตัวเดียว · **เกต sandbox จึงประกอบแผงสามตระกูลไม่ได้
  บนแพลตฟอร์มเดียวที่มันรันได้** · อธิบายว่าทำไม macOS ต้องใช้ ACP ตรง

**9 ใบเดิมปิดหมดแล้ว** — อย่าไปเปิดใหม่โดยไม่อ่าน comment ที่ปิดไว้ ทุกใบมีเหตุผลเขียนไว้

## สี่โดเมน — วัดใหม่ 8 ส.ค.

| โดเมน | สถานะ |
|---|---|
| **token** | **ครบ** — ประตูของคนสามบาน: `admit` · `answer` · `withdraw` |
| **team** | ฮาร์ดโค้ด 8 จุดใน `nextStep` — **และไม่มีใบปัญหาไหนขอให้เปลี่ยน** |
| **workflow** | **เป็นเจ้าของการส่งต่อแล้ว** — `pull-controller.mjs:154-200` |
| **display** | **จัดชั้นถูกอยู่แล้ว** — `pulse.json` คือหลักฐาน (สัญญา §11.1) |

**แผนงานเดิมเขียนผิดสามในห้าข้อ** และตายด้วยการวัดภายในชั่วโมงเดียว:
~~workflow ไม่เป็นโดเมน~~ · ~~runner พึ่ง display~~ · ~~ทีมต้องประกาศกระบวนการเอง~~

**บทเรียน: อย่าวัดจุดเดียวแล้วสรุปทั้งโดเมน**

**MQ ทำแล้วบางส่วน ไม่ใช้ broker** · แหล่งอีเวนต์จริงมี 3 ทาง: worker เขียน outbox ·
คนสั่งผ่าน 3 ประตู · **นาฬิกา (เส้นตาย 5 ตัว)** · สองในสามคือไฟล์เปลี่ยน ·
**interval ยังอยู่เพราะเป็นผู้อ่านเดียวของนาฬิกา** · POC ใน scratchpad พิสูจน์ at-most-once
หาย 1/60 — **จริงแต่ไม่ใช่ปัญหาของเรา** สมุดต่อท้ายอย่างเดียวหายไม่ได้

### หลักการตัดสินใจของโปรเจกต์ — Master, 2026-08-07

> **ทำสิ่งที่จำเป็น ในเวลาที่จำเป็น เท่าที่จำเป็น**

สามด่าน: **จำเป็นไหม · ต้องตอนนี้ไหม · เท่านี้พอไหม** — รันก่อนถาม Master
มันไม่ตอบคำถามเรื่อง**ข้อเท็จจริง** — พวกนั้นต้องวัด

### หัวใจของระบบ — Master, 2026-08-06

> งานติดที่ทีม = WIP ค้างที่ทีม · **escalate ขึ้น PM = WIP ของ PM ค้างจนเสร็จ** ·
> **ทรงพลังเพราะทำให้ระบบหยุดเมื่อมีปัญหา**

**ตอนนี้เป็นจริงแล้ว** — D6 ทำให้ escalate/questioned กินช่อง control จริง

## ที่ตัดสินแล้ว ห้ามรื้อ

- **D1** ขา audit ที่ตายที่ transport **ค้างไว้** คนมาปลด
- **D2** การกู้คือ **คำถาม** ไม่ใช่คำใหม่
- **D6 สร้างแล้ว** — กราฟไม่มีทีม control ปฏิเสธตั้งแต่โหลด · ยอมแก้ 212 เทสใน 17 ไฟล์
  · **เทส 5 ตัวที่ตรึงโลกเก่า เขียนใหม่ ไม่ลบ**
- **ทางออกงานตีปิงปอง = ทาง B** (Master 8 ส.ค.) · **ไม่ทำ `override.mjs`** เพราะย้อนทางไหล
  · `withdraw.mjs` แล้วเปิดใบใหม่ ตรงกับที่ §6.3 เขียนไว้เอง
- **`PLACES_BY_DESTINATION = {resumed}` เท่านั้น** · `escalated` ไม่อยู่โดยตั้งใจ — escalate คืองาน PM
- **log ทำให้ grep ปลอดภัยไม่ได้** โดยไม่ทำลายข้อความ — **และไม่ต้องทำ** เพราะ log ไม่ใช่พยาน
  · `[terminal]` เป็นบรรทัด **diagnostic** · คำตัดสินคือไฟล์ outbox ที่ผูก `outbox_digest`
  · บรรทัดนี้เคยเขียนว่า "คือบรรทัดเดียวที่บอกคำตัดสิน" — **ผิด และถูกลบออกจากโค้ดแล้ว
  (`d18fbbb`) แต่รอดอยู่ในไฟล์นี้จนผู้รีวิวจับได้** ดูหัวข้อ ✅ ทาง C

## DO NOT — พลาดมาแล้ว อย่าทำซ้ำ

1. **`grep -c` นับบรรทัด ไม่ใช่จำนวนครั้ง** — ใช้ `s.count()`
2. **ไดเรกทอรีใหม่ทุกเลน ACP** — receipt ไม่แยกตามเลน
3. **`liveness/*.json` เก็บ session id ไม่ครบ (20 ตัว)** — ตัวเต็มอยู่ใน stdout ของ companion
4. **วัดเลขบรรทัดใหม่ทุกครั้งหลังแตะไฟล์**
5. **แก้ประโยคที่ผิด อย่าล้อมรั้วด้วยกล่องแก้**
6. **อย่า commit ต่อท้าย test run ด้วยขึ้นบรรทัดใหม่** — ใช้ `&&` · **commit บนแดงมาแล้ววันนี้**
7. **mutation ที่ไม่ได้แทนอะไรแล้วรายงาน "เทสจับไม่ได้" คือคำโกหกที่แนบเนียนที่สุด**
   — assert ว่า anchor ตรงเสมอ · ถ้า escaping ยุ่ง ใช้ `repr()` ดูบรรทัดจริง
8. **อย่า `npm cache clean --force`** ถ้ายังต้องใช้เลนรีวิว — ลบ adapter ที่ยืนยันแล้ว
9. **เทสที่รอเวลาตายตัวคือ flake** — รอ**เงื่อนไข** และถ้ามี debounce **อย่าเขียนถี่กว่า debounce**

## 🟢 ปิดแล้ว (คนละทางกับที่ตั้งใจ) — `acp-companion` fifo แดงหนึ่งครั้ง 8 ส.ค.

**`an outbox that is not a regular file is refused rather than read` (เคส `fifo`)**
แดงหนึ่งครั้งในรอบ suite เต็ม (791/1) · **ไม่ใช่ timeout** — ล้มที่ 2.2 วิ จาก 20

- รันเดี่ยว **132/132** · repro ตรง **168 รอบ** (12 × 6-8 ขนาน ทั้งแบบ env เต็มและ env
  ของ suite เป๊ะ ๆ) — **ไม่ติดสักครั้ง**
- ที่ล้มคือ `assert.match(stderr, /symlink|not a regular file/)` — **status ไม่ใช่ 0
  อยู่แล้ว** แปลว่าโปรแกรมปฏิเสธ แต่ปฏิเสธด้วย**ข้อความอื่น**
- **สมมติฐาน ยังไม่พิสูจน์:** `spawnSync('mkfifo')` ใน fixture ล้ม (fork ถูกปฏิเสธใต้โหลด
  ของ suite) → ไม่มีไฟล์ที่ปลายทางเลย → companion ตอบ "no outbox" ซึ่งเป็นคนละประโยค
- **แก้ไปแล้วสองอย่าง ถูกต้องไม่ว่าสาเหตุจริงคืออะไร:** fixture **โยน** เมื่อ `mkfifo`
  ล้ม (เดิมกลืนเงียบ) · assertion พิมพ์ `stderr` จริงออกมา (เดิมพิมพ์แค่คำว่า `fifo`
  รายงานจึงอ่านว่า `AssertionError: fifo` และไม่บอกอะไรเลย)
- **แล้วรอบ 4 พบว่าการแก้นั้นยังผิด** — วาง fifo ไว้ที่ตำแหน่ง outbox ทำให้
  `clearStaleOutbox()` ปฏิเสธก่อน `session/prompt` **`readTerminalOutbox` ไม่เคยรัน**
  และสองจุดพ่นประโยคเดียวกัน เทสจึงเขียวขณะทดสอบคนละเรื่อง · ตอนนี้ stage ไว้นอก
  `.mailbox-out` แล้ว rename เข้ามาระหว่างเทิร์น · **ยืนยันด้วย mutation ของผู้รีวิวเอง**
- **สาเหตุของแดงครั้งเดียวนั้นยังไม่พิสูจน์ และไม่อ้างว่าพิสูจน์แล้ว**
- **กฎของเรพ: ห้ามนับว่าเป็น noise**

## บทเรียนหลักของรอบนี้ — อ่านก่อนแก้อะไรที่เป็น parser หรือ anchor

**7 ใน 13 finding ของ codex สามรอบหลัง ไม่ใช่บั๊กในโค้ด — เป็นประโยคที่กว้างกว่าโค้ด**
คอมเมนต์ · ชื่อเทส · บรรทัดในสัญญา · ข้อความใน log ทั้งหมดเขียนโดยเราเอง
และทั้งหมด**คนอ่านเจอ ไม่ใช่เทสเจอ**

สามคำถามที่ต้องถามตัวเองทุกครั้งก่อนบอกว่าเสร็จ:

1. **ประโยคนี้กว้างกว่าโค้ดไหม** — ถ้าเขียนว่า "ทุกชั้น" ไปนับให้ครบทุกชั้นก่อน
2. **เทสนี้แตะสิ่งที่มันตั้งชื่อจริงไหม** — เขียวไม่ได้แปลว่าถูกเรียก · วิธีเดียวที่ตัดสินได้
   คือ **mutation**: ลบเช็คที่มันควรจับ แล้วดูว่าแดงไหม
3. **การแก้นี้เพิ่งเปิดคลาสอะไร** — ถามทันทีหลังแก้ ไม่ใช่รอรีวิวบอก (ครั้งนี้ได้ 9 รูรั่วฟรี)
4. **การวัดที่เพิ่งรัน ตอบได้แค่สิ่งที่มันไปแตะ** — เพิ่ม 8 ส.ค. รอบ 7 · เจ็บสองครั้งในวันเดียว:
   - `grep 'only line stating a verdict'` เจอจุดเดียว **ผมสรุปว่า "ไม่มีที่อื่น"** ของจริงเขียนว่า
     `The ONLY line in this stream that states a verdict` + `A supervisor greps THIS` —
     **คนละถ้อยคำ เจตนาเดียวกัน สองในสามเลนเจอ ผมไม่เจอ**
   - mutation ฉีดแต่ `.mjs` · guard เดินแต่ `.mjs` · มี `workflows/mailbox-run.js` ship อยู่
     **เขียวทั้งคู่ และทั้งคู่พิสูจน์คนละเรื่องกับที่คอมเมนต์อ้าง**

   **วิธีเดียวที่ใช้ได้: ค้นด้วยเจตนา ไม่ใช่ถ้อยคำ** (`grep -i 'supervisor\|only.*verdict\|grep'`)
   และ **นับสิ่งที่ walker ข้ามไป** ก่อนเขียนคำว่า "ทั้งต้นไม้"

## ถัดไป ตามลำดับ

**ไม่มีอะไรค้างที่ต้องรีบ** — เลือกจาก Roadmap ข้างบน ไม่ใช่จากรายการนี้

1. **เฟส A — กำลังทำ** · `mcpServers: []` ต้องมีเทสตอนรัน (ทั้ง `session/new` และ
   `session/load`) · และเชื่อ `assigned` (โมเดลที่ขอ) กับ `identity_status` (โมเดลที่ตอบ)
   ให้ขัดกันได้ · **ห้ามขยายเกินสองข้อนี้**
2. ⚠️ **รีลีสถัดไปติดเกตเต็มรูปแน่นอน** — เฟส A แตะ `plugins/**` · รัน
   `node scripts/gate-required.mjs` แล้วอ่านรายชื่อไฟล์ที่มันชี้ ก่อนคิดจะบัมพ์เวอร์ชัน
3. 🔵 **ขอ Master ก่อนแตะ `~/agent-skills`** — submodule pin ยังชี้ sha เก่า · คนละโปรเจกต์
   · ปักที่ sha ล่าสุดของ `main` เมื่อได้อนุญาต (OpenClaw bridge ใช้หมุดนี้)

### ขั้นตอนปล่อยรีลีส (ทำมาแล้วสามครั้งติดในเซสชันนี้)

0. **`node scripts/gate-required.mjs`** ก่อนอย่างอื่น — `2` = ต้องรันแผงสามโมเดลก่อนสแตมป์เลข
   · `0` = ยกเว้นได้ **แต่ต้องเขียนบรรทัด `Gate:` ลง release notes ทั้งสองกรณี**
1. บัมพ์ **5 ไฟล์ · 6 จุด** — `marketplace.json` **2 จุด** ·
   `plugins/tmux-teams/.claude-plugin/plugin.json` · `plugins/tmux-teams/plugin.json` ·
   `tests/plugin-structure.test.mjs` · `README.md`
   **แล้ว `git grep` หาเลขเก่าเสมอ** — จำนวนจุดเพิ่มขึ้นมาแล้วสามครั้ง และ**ทุกครั้งคนเจอก่อนระบบ**
2. `node --test` + `git diff --check` + `claude plugin validate --strict .`
3. **push** (`gh auth switch --user iicmaster` ก่อน) แล้วเช็ค `git status -sb` ว่า**ไม่มี ahead**
   — ไม่ใช่แค่ดูว่า push พิมพ์ hash ออกมา · **ขออนุญาต Master ก่อน push ทุกครั้ง**
4. **`gh run watch <id> --exit-status` — ห้าม tag ตัวแดง**
5. `git tag vX.Y.Z && git push origin vX.Y.Z` แล้ว `gh release create` ด้วย `--notes-file`
   (ห้าม `printf` — backfill 30 ใบเคยได้ `%ad` ติดไปทั้งหมด)
6. `claude plugin marketplace update tmux-teams` แล้ว `claude plugin update tmux-teams@tmux-teams`

## หนี้ที่รู้ตัว

- **`docs/` ยังไม่ commit** — ไม่อยู่ในรายการ tracked (ตั้งใจ) · เอกสารสองชุดอยู่ที่
  `artifacts.ngs.bz/claude/private/{tmux-teams-ddd-reading,tmux-teams-next-plan}/`
  · **ต้องมี `<meta charset="utf-8">` เสมอ** ไม่งั้นภาษาไทยเพี้ยนทั้งหน้า
- **submodule pin ใน `~/agent-skills`** ยังไม่อัปเดต · **คนละโปรเจกต์ ต้องขอ Master**
- **`~/.cache` โต ~6.5GB** (`uv` 3.6G · `codex-runtimes` 1.5G) — ดิสก์เต็มสองครั้งวันนี้
  **และครั้งหนึ่งทำ ACP lane ตายกลางรีวิว**
- **worktree ค้างใน `.claude/worktrees/`** — หนี้พื้นที่ ไม่ใช่หนี้ความถูกต้อง
